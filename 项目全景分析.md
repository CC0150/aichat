# 项目全景分析：Intervy - AI 面试助手

> 一份完整覆盖项目每一层架构、每一个核心文件、每一条数据流的分析文档。
> 写完代码再回来看这张地图，你才知道自己在哪。

---

## 目录

- [一、一句话概括](#一一句话概括)
- [二、技术全景](#二技术全景)
- [三、项目结构图](#三项目结构图)
- [四、启动链路](#四启动链路)
- [五、数据流全景](#五数据流全景)
- [六、后端逐层拆解](#六后端逐层拆解)
  - [6.1 配置层](#61-配置层-serverconfigindexjs)
  - [6.2 中间件层](#62-中间件层-servermiddlewareindexjs)
  - [6.3 路由层](#63-路由层-serverroutes)
  - [6.4 服务层](#64-服务层-serverservices)
  - [6.5 工具层](#65-工具层-serverutils)
- [七、前端逐层拆解](#七前端逐层拆解)
  - [7.1 启动入口](#71-启动入口-srcmainjs)
  - [7.2 路由](#72-路由-srcrouterindexjs)
  - [7.3 状态管理](#73-状态管理-srcstores)
  - [7.4 API 层](#74-api-层-srcutils)
  - [7.5 虚拟滚动](#75-虚拟滚动-usevirtualscrollheightjs)
  - [7.6 语音输入](#76-语音输入-usespeechrecognitionjs)
  - [7.7 文件解析](#77-文件解析-docparserjs)
- [八、四种 AI 交互模式深度剖析](#八四种-ai-交互模式深度剖析)
- [九、面试系统](#九面试系统)
- [十、知识库系统](#十知识库系统)
- [十一、主题与 UI 系统](#十一主题与-ui-系统)
- [十二、关键设计决策](#十二关键设计决策)
- [十三、当前局限与演进方向](#十三当前局限与演进方向)
- [附录：依赖全景](#附录依赖全景)

---

## 一、一句话概括

**Intervy** 是一个 Vue 3 + Express 的全栈 AI 面试练习平台。核心链路：

```
用户在前端操作 → Pinia 管理状态（自动持久化 localStorage）
  → API 请求通过 Vite 代理转发到 Express 后端
  → 后端通过 OpenAI SDK 调 DeepSeek API
  → 结果返回前端渲染
```

---

## 二、技术全景

```
┌──────────────────────────────────────────────────┐
│                    前端                           │
│                                                  │
│  Vue 3 (Composition API)                         │
│  ├─ Pinia (状态管理，自动持久化 localStorage)      │
│  ├─ Vue Router 4 (5 页，全懒加载)                 │
│  ├─ Tailwind CSS 3 (CSS 变量主题系统)              │
│  ├─ vue3-virtual-scroller (虚拟滚动)              │
│  ├─ Chart.js (雷达图 / 柱状图)                    │
│  ├─ markdown-it + highlight.js (AI 回复渲染)      │
│  ├─ pdfjs-dist + mammoth (文件解析)               │
│  ├─ floating-vue (Tooltip)                       │
│  └─ Web Speech API (语音输入)                     │
│                                                  │
├──────────────────────────────────────────────────┤
│                    后端                           │
│                                                  │
│  Express 4                                       │
│  ├─ helmet (安全头，含 CSP)                       │
│  ├─ cors (跨域)                                   │
│  ├─ compression (Gzip)                           │
│  ├─ express-rate-limit (30/min/IP)               │
│  ├─ OpenAI SDK v6 (对接 DeepSeek)                 │
│  └─ 文件系统存储 (知识库)                          │
│                                                  │
├──────────────────────────────────────────────────┤
│                  AI 层                            │
│                                                  │
│  DeepSeek API (兼容 OpenAI 协议)                   │
│  ├─ deepseek-v4-flash (默认，快)                  │
│  └─ deepseek-v4-pro (更强推理)                    │
│                                                  │
└──────────────────────────────────────────────────┘
```

---

## 三、项目结构图

> ★ = 核心文件，理解整个项目必须读的

```
aichat/
├── vite.config.js                     # ★ Vite 配置：@ 别名 + /api 代理到 3001
├── tailwind.config.js                 # Tailwind 主题（CSS 变量 → utility class）
├── index.html                         # Vite 入口
├── package.json                       # 前端依赖
│
├── src/                               # ===== 前端源码 =====
│   ├── main.js                        # ★ Vue 启动入口（Pinia→Router→FloatingVue→VirtualScroll）
│   ├── App.vue                        # 根组件
│   ├── assets/
│   │   └── theme.css                  # ★ 主题 CSS 变量（:root 浅色 / .dark 深色）
│   │
│   ├── router/
│   │   └── index.js                   # ★ 5 个路由，全懒加载，meta.title → 标签页标题
│   │
│   ├── stores/                        # ★ Pinia（全持久化到 localStorage）
│   │   ├── app.js                     #   主题 / 侧边栏 / 当前模型
│   │   ├── chat.js                    #   聊天会话 / 消息 / 撤销 / 重新生成
│   │   ├── interview.js               #   面试状态机 / 评分 / 追问 / 历史
│   │   └── knowledge.js               #   知识库 CRUD
│   │
│   ├── views/                         # 页面（全懒加载）
│   │   ├── ChatView.vue               #   自由对话
│   │   ├── InterviewView.vue          #   AI 面试（配置→答题→结果）
│   │   ├── StatsView.vue              #   面试统计（图表）
│   │   └── KnowledgeView.vue          #   知识库管理
│   │
│   ├── components/
│   │   ├── AppLayout.vue              #   布局框架
│   │   ├── AppSidebar.vue             #   侧边栏（会话列表+搜索）
│   │   ├── ChatHeader.vue             #   聊天顶栏
│   │   ├── ChatInput.vue              #   输入框（模型切换/语音/发送/停止）
│   │   ├── MessageArea.vue            #   消息列表（虚拟滚动）
│   │   ├── MarkdownContent.vue        #   AI 回复渲染（代码高亮+复制）
│   │   ├── Modal.vue                  #   通用弹窗
│   │   └── interview/
│   │       ├── InterviewSession.vue   #   ★ 面试核心流程
│   │       ├── InterviewConfig.vue    #   出题配置面板
│   │       ├── InterviewResult.vue    #   单题结果展示
│   │       └── ...
│   │
│   ├── composables/                   # 组合式函数
│   │   ├── useVirtualScrollHeight.js  #   ★ 虚拟滚动高度管理
│   │   └── useSpeechRecognition.js    #   语音输入
│   │
│   ├── utils/                         # 工具函数
│   │   ├── chatApi.js                 #   ★ SSE 流式请求
│   │   ├── apiClient.js               #   ★ 统一 fetch 封装
│   │   ├── interviewApi.js            #   面试 API
│   │   ├── knowledgeApi.js            #   知识库 API
│   │   ├── interviewHelpers.js        #   评分颜色/难度标签
│   │   ├── interviewExport.js         #   记录导出
│   │   ├── modelConfig.js             #   模型定义
│   │   ├── messageBuilder.js          #   messages 数组构建
│   │   ├── docParser.js               #   ★ PDF/Word/TXT 解析
│   │   ├── tokenCounter.js            #   Token 估算
│   │   └── textarea.js                #   输入框自动高度
│   │
│   └── data/
│       └── questions/                 # ★ 内置题库
│           ├── index.js               #   聚合 + selectQuestions
│           ├── html.js / css.js / js.js / vue.js / react.js / engineering.js
│
├── server/                            # ===== 后端源码 =====
│   ├── index.js                       # ★ 服务入口
│   ├── .env                           # ★ 环境变量
│   ├── package.json
│   │
│   ├── config/
│   │   └── index.js                   # ★ OpenAI 实例 + 模型白名单
│   │
│   ├── middleware/
│   │   └── index.js                   # ★ 中间件链 + SSE 头 + 全局兜底
│   │
│   ├── routes/
│   │   ├── index.js                   # ★ 路由注册中心
│   │   ├── chat.js                    # ★ SSE 聊天
│   │   ├── interview.js               # ★ 评分 + 追问
│   │   ├── questions.js               # ★ AI 出题
│   │   ├── knowledge.js               # ★ 知识库 CRUD + 出题
│   │   └── health.js                  # 健康检查
│   │
│   ├── services/
│   │   ├── deepseek.js                # ★ streamChat (异步生成器)
│   │   ├── aiCompletions.js           # ★ callAI (非流式 → JSON)
│   │   └── errorHandler.js            # ★ 统一 AI 错误处理
│   │
│   ├── utils/
│   │   ├── parseJson.js               # ★ extractJson
│   │   ├── validate.js                # ★ 输入校验
│   │   └── constants.js               # 难度映射
│   │
│   └── data/
│       └── knowledge/                 # ★ 知识库文件存储
│           ├── index.json
│           └── {kb-id}/meta.json + files/
│
└── test/
    └── smoke.test.js
```

---

## 四、启动链路

### 首次打开页面时发生了什么

```
1. 用户访问 http://localhost:5173
2. Vite 返回 index.html → 加载 src/main.js
3. main.js 执行：
   a. 创建 Pinia + pinia-plugin-persistedstate
      → 自动从 localStorage 恢复 app/chat/interview/knowledge 四个 store
   b. 读 localStorage['app'] 拿到主题设置
      → 立即设置 html.dark class（在 Vue 挂载前，防闪烁）
   c. use(pinia) → use(router) → use(FloatingVue) → use(VirtualScroller)
   d. app.mount('#app')
4. Router 匹配当前 URL → 懒加载对应 View 组件
5. View 组件 setup → 从 Pinia store 读取数据 → 渲染页面
```

### 后端启动时发生了什么

```
1. dotenv.config({ path: 'server/.env' }) → 加载环境变量
2. config/index.js 被 require → 创建 OpenAI 实例（baseURL 指向 DeepSeek）
3. 检查 DEEPSEEK_API_KEY 是否存在，不存在只 warn 不退出
4. setupMiddleware(app) → 依次注册 5 个中间件
5. setupRoutes(app) → 注册 5 组路由
6. setupErrorHandler(app) → 注册全局错误处理（必须在路由之后）
7. app.listen(PORT) → 开始监听
```

### 两个服务如何通信

```
浏览器请求 /api/chat
  → Vite Dev Server (5173) 收到请求
  → 匹配 proxy 规则: '/api' → 'http://localhost:3001'
  → 转发到 Express (3001)
  → Express 处理后返回
  → Vite 透传给浏览器

生产环境:
  → Express 直接 serve dist/ 静态文件
  → 或 Nginx 反向代理
```

---

## 五、数据流全景

```
┌──────────────────────────────────────────────────────────────────┐
│                         浏览器                                    │
│                                                                  │
│  Pinia Stores (localStorage 持久化)                               │
│  ┌────────────┐ ┌───────────┐ ┌────────────┐ ┌───────────────┐  │
│  │ app store  │ │chat store │ │interview   │ │knowledge store│  │
│  │ 主题/侧栏  │ │会话/消息  │ │状态机/评分 │ │KB 列表/详情   │  │
│  └────────────┘ └───────────┘ └────────────┘ └───────────────┘  │
│        │              │              │               │           │
│        ▼              ▼              ▼               ▼           │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                   utils/ API 封装层                        │   │
│  │  chatApi.js          interviewApi.js    knowledgeApi.js   │   │
│  │  (SSE ReadableStream) (apiClient 封装)   (apiClient 封装) │   │
│  └──────────────────────────┬───────────────────────────────┘   │
│                             │                                    │
└─────────────────────────────┼────────────────────────────────────┘
                              │  /api/* 请求
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│                  Vite Dev Server (5173)                           │
│  proxy: { '/api': 'http://localhost:3001' }                      │
└─────────────────────────────┬────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│                    Express (3001)                                 │
│                                                                  │
│  中间件链:                                                        │
│  compression → helmet → cors → json(5mb) → rateLimit(30/min)    │
│                                                                  │
│  路由分发:                                                        │
│  ┌─────────────┐ ┌──────────────┐ ┌───────────────┐             │
│  │/api/chat    │ │/api/interview│ │/api/questions │             │
│  │ SSE 流式    │ │ JSON 评分/   │ │ AI 出题       │             │
│  │             │ │ 追问         │ │               │             │
│  └──────┬──────┘ └──────┬───────┘ └───────┬───────┘             │
│         │               │                 │                      │
│  ┌──────┴──────┐ ┌──────┴───────┐ ┌───────┴───────┐             │
│  │/api/knowledge│ │/health      │                               │
│  │知识库 CRUD   │ │健康检查     │                               │
│  │+ 出题        │ │             │                               │
│  └──────┬──────┘ └─────────────┘                                │
│         │                                                        │
│         ▼                                                        │
│  server/data/knowledge/ (文件系统)                                │
│                                                                  │
│  AI 调用服务:                                                     │
│  ┌─────────────────┐  ┌──────────────────┐                      │
│  │ deepseek.js     │  │ aiCompletions.js │                      │
│  │ streamChat      │  │ callAI           │                      │
│  │ (流式，聊天)    │  │ (非流式，评分等) │                      │
│  └────────┬────────┘  └────────┬─────────┘                      │
│           │                    │                                  │
└───────────┼────────────────────┼──────────────────────────────────┘
            │                    │
            ▼                    ▼
┌──────────────────────────────────────────────────────────────────┐
│            DeepSeek API (api.deepseek.com/v1)                    │
│                                                                  │
│  POST /v1/chat/completions                                       │
│    stream: true  → SSE 流式                                      │
│    stream: false → 一次性 JSON                                    │
│                                                                  │
│  模型: deepseek-v4-flash (默认)  /  deepseek-v4-pro              │
│                                                                  │
│  通过 OpenAI SDK v6 调用，配置 baseURL 指向 DeepSeek              │
└──────────────────────────────────────────────────────────────────┘
```

---

## 六、后端逐层拆解

### 6.1 配置层 `server/config/index.js`

```js
// 环境变量读取
PORT          = process.env.PORT || 8787          // 注意：nginx 默认 3001
API_KEY       = process.env.DEEPSEEK_API_KEY
BASE_URL      = process.env.DEEPSEEK_BASE_URL || 'https://api.deepseek.com/v1'
DEFAULT_MODEL = process.env.DEEPSEEK_MODEL || 'deepseek-v4-flash'

// 模型白名单 — 只有两个合法值
VALID_MODELS  = Set(['deepseek-v4-flash', 'deepseek-v4-pro'])

// OpenAI SDK 实例
openai = new OpenAI({ apiKey, baseURL, timeout: 60000 })

// 模型名校验 — 非法值静默回退
sanitizeModel(model) = VALID_MODELS.has(model) ? model : DEFAULT_MODEL
```

**设计意图**：
- `sanitizeModel` 确保前端传任何模型名都不会导致 404
- `timeout: 60000` 防止长时间挂起
- `baseURL` 默认指向 DeepSeek，但可改为任何 OpenAI 兼容 API

### 6.2 中间件层 `server/middleware/index.js`

**注册顺序就是执行顺序**，每个请求都会按这个链条走一遍：

```
compression()       → 响应体 Gzip 压缩，减少带宽
helmet({ CSP })     → 安全 HTTP 头
                       CSP 放行: iconify (图标)、data: (base64 图片)、https: (图床)
cors('*')           → Allow all origins（开发期方便）
express.json(5mb)   → 解析 JSON body，上限 5MB
rateLimit           → /api 路径: 1 分钟窗口内最多 30 次请求
```

**`writeSSEHeaders(res)`** — SSE 专用响应头设置：

```js
Content-Type: text/event-stream; charset=utf-8   // 事件流类型
Cache-Control: no-cache, no-transform             // 禁止缓存
Connection: keep-alive
X-Accel-Buffering: no                             // 禁止 Nginx 缓冲

// 2KB 防缓冲填充 — 防止某些反向代理等完整响应才转发
res.write(': ' + ' '.repeat(2048) + '\n\n')
```

**全局错误处理** — 捕获所有未处理的异常，返回 500/502，防止进程崩溃。生产环境隐藏错误详情。

### 6.3 路由层 `server/routes/`

#### 路由注册 `index.js`

```js
setupRoutes(app) {
  app.use('/api/chat',      chatRouter)
  app.use('/api/interview', interviewRouter)
  app.use('/api/questions', questionsRouter)
  app.use('/api/knowledge', knowledgeRouter)
  app.use('/health',        healthRouter)
}
```

#### chat.js — SSE 流式聊天

```
POST /api/chat { model?, messages }

流程：
  1. writeSSEHeaders(res) → 写 SSE 响应头
  2. sanitizeModel(rawModel) → 校验模型名
  3. 校验 messages 不为空
  4. for await (chunk of streamChat(model, messages)):
       res.write(`data: ${JSON.stringify({ content: chunk })}\n\n`)
  5. res.write("data: [DONE]\n\n")
  6. res.end()

错误：res.write({ error: '...' }) → res.end()
```

**SSE 数据格式**：
```
data: {"content":"你"}\n\n
data: {"content":"好"}\n\n
data: {"content":"，"}\n\n
data: [DONE]\n\n
```

#### interview.js — 评分 + 追问

**两个端点，两种 Prompt 模板**：

```
POST /api/interview/score    — 单次评分，temperature=0.3
  Prompt 变量: {question}, {answerPoints}, {userAnswer}
  AI 返回: { score, correctness, completeness, clarity, feedback, improvedAnswer }

POST /api/interview/evaluate — 多轮追问，temperature=0.3
  Prompt 变量: {question}, {answerPoints}, {history}, {maxRounds}, {currentRounds}
  AI 返回: { action: "follow_up", followUpQuestion, scoreHint }
       或: { action: "complete", score, correctness, ... }
```

**追问逻辑**（当前是硬编码的）：
```
MAX_ROUNDS = 3
currentRounds = conversationHistory.length / 2  // 一问一答算一轮

if (currentRounds >= MAX_ROUNDS) → forceComplete 提示
  否则 → AI 自主决定 follow_up 还是 complete
```

**这就是 Agent 改造的切入点**：把硬编码的 3 轮上限替换为 AI 自主决策。

#### questions.js — AI 出题

**两个端点**：

```
POST /api/questions/generate           — 从文件内容出题
  body: { content, questionCount, difficulty? }
  处理: content 超过 8000 字符硬截断 → 拼模板 → callAI → 返回题目数组

POST /api/questions/generate-by-role   — 按目标岗位出题
  body: { role, questionCount, difficulty? }
  处理: 不需要 content，AI 用自身知识出题
```

**返回的题目结构**：
```json
{
  "type": "concept|code|scenario",
  "category": "vue",
  "difficulty": "medium",
  "tags": ["响应式"],
  "knowledgePoints": ["Proxy", "Reflect"],
  "question": "Vue 3 响应式原理？",
  "answerPoints": ["基于 Proxy..."]
}
```

#### knowledge.js — 知识库 CRUD + 出题

| 操作 | 路由 | 核心逻辑 |
|------|------|---------|
| 列表 | GET `/` | 读 `index.json` |
| 创建 | POST `/` | 生成 `kb-id` → 写 `meta.json` → 更新 `index.json` |
| 详情 | GET `/:id` | 读 `meta.json` |
| 删除 | DELETE `/:id` | 递归删目录 → 更新 `index.json` |
| 上传文件 | POST `/:id/files` | 校验 `content`（最大 100000 字符）→ 写 `{file-id}.txt` → 更新 `meta.json` |
| 删除文件 | DELETE `/:id/files/:fileId` | 删 `.txt` → 更新 `meta.json` |
| 出题 | POST `/:id/generate` | 聚合所有文件内容 → 超过 10000 字符硬截断 → `callAI` |

**出题时的截断问题**（RAG 要解决的）：
```js
const MAX_CONTENT = 10000
if (combined.length > MAX_CONTENT) {
  combined = combined.slice(0, MAX_CONTENT)  // 后面的内容全部丢失
}
```

### 6.4 服务层 `server/services/`

#### deepseek.js — 流式 AI 调用

```js
async function* streamChat(model, messages) {
  const stream = await openai.chat.completions.create({
    model, messages, stream: true
  })
  for await (const chunk of stream) {
    const content = chunk.choices?.[0]?.delta?.content
    if (content != null && content !== '') yield String(content)
  }
}
```

- 异步生成器（AsyncGenerator），调用方用 `for await` 消费
- `stream: true` → DeepSeek 逐 token 返回
- 只 yield 非空的 content 片段

#### aiCompletions.js — 非流式 AI 调用

```js
async function callAI({ model, prompt, temperature, maxTokens, logTag }) {
  const response = await openai.chat.completions.create({
    model,
    messages: [{ role: 'user', content: prompt }],  // 注意：只有一条 user 消息
    temperature,
    max_tokens: maxTokens
  })
  const raw = response.choices[0]?.message?.content || ''
  return JSON.parse(extractJson(raw))
}
```

- 你的评分/出题/追问全都走这个函数
- `prompt` 直接作为 user 消息内容，不使用 system role
- `extractJson` → `JSON.parse` 是核心后处理

#### errorHandler.js — 统一错误响应

```js
function handleAIError(res, err, logTag, { fallbackMessage, extras } = {}) {
  console.error(`[${logTag}] 失败:`, err.message)
  if (err instanceof SyntaxError) console.error(`[${logTag}] JSON 解析失败`)
  res.status(502).json({
    error: err?.message || fallbackMessage,
    retryable: true,
    ...extras
  })
}
```

- 统一返回 502（上游 AI 服务异常）
- `retryable: true` 告诉前端可以重试
- 区分 JSON 解析错误（打印额外日志）
- `extras` 参数允许路由在错误时返回兜底数据（如 `{ questions: [] }`）

### 6.5 工具层 `server/utils/`

#### parseJson.js — 从 AI 返回中提取 JSON

```
输入可能是:
  "这是一些解释文字\n```json\n{"score":8}\n```\n还有其他"
  "直接返回的 JSON {\"score\": 8}"
  "[\n{\"question\":\"...\"}\n]"

extractJson() 做的事:
  1. 匹配 ```json ... ``` 代码块，去壳
  2. 找第一个 { 和最后一个 } → 提取对象
  3. 找第一个 [ 和最后一个 ] → 提取数组
  4. 返回最外层合法结构

失败时 → JSON.parse() 抛 SyntaxError → handleAIError 捕获
```

#### validate.js — 输入校验

```js
sanitizeString(value, { maxLength, required })
  → trim → 长度截断 → 空值判断

validateEnum(value, allowed, default_)
  → 白名单校验

clampNumber(value, min, max, default_)
  → 取整 → 钳制 → 兜底
```

**所有路由都用这三个函数做第一道防线**，防止恶意输入或超大 payload 导致 API 调用异常。

#### constants.js

```js
DIFFICULTY_MAP = {
  all:    '简单 40%、中等 40%、困难 20%',
  easy:   '全部为简单难度',
  medium: '全部为中等难度',
  hard:   '全部为困难难度'
}
```

拼入出题 prompt，告诉 AI 难度分布期望。

---

## 七、前端逐层拆解

### 7.1 启动入口 `src/main.js`

```js
执行顺序:

1. createPinia() + piniaPluginPersistedstate
   → 从 localStorage 恢复所有 store 数据

2. safeLocalStorage 挂到 window
   → 兼容 Safari ITP 隐私模式

3. 主题防闪烁（在 Vue 挂载前执行！）
   → 读 localStorage['app'] → 解析 JSON → 设置 html.dark class

4. createApp(App)
   → use(pinia)        // 状态管理
   → use(router)       // 路由
   → use(FloatingVue)  // Tooltip（200ms 延迟）
   → use(VirtualScroller) // 虚拟滚动
   → mount('#app')

5. Router afterEach
   → document.title = '页面标题 - Intervy'
```

**主题防闪烁为什么重要**：如果在 Vue 挂载后才发现需要切深色模式，用户会先看到白色背景闪烁一下再变黑。通过在 `main.js` 顶部、Vue 实例创建前就设置 `html.dark`，消除了这个闪烁。

### 7.2 路由 `src/router/index.js`

```
/                          → ChatView.vue      标题: 对话 - Intervy
/chat/:id?                 → ChatView.vue      标题: 对话 - Intervy
/interview                 → InterviewView.vue 标题: AI 面试 - Intervy
/stats                     → StatsView.vue     标题: 面试记录 - Intervy
/knowledge                 → KnowledgeView.vue 标题: 知识库 - Intervy

全部懒加载: () => import('@/views/...')
首屏只下载当前页面代码，其他页面用时才加载
```

两个 Chat 路由指向同一个组件，`/chat/:id` 带会话 ID 时直接打开指定会话，不加时创建新会话。

### 7.3 状态管理 `src/stores/`

#### app store — 全局 UI

```js
状态:
  sidebarCollapsed      桌面侧边栏是否折叠
  sidebarOpen           移动端抽屉是否打开
  isDark                深色/浅色（watch 自动同步 DOM + localStorage）
  currentModelId        当前模型 ID（启动时校验合法性）

操作:
  toggleSidebar()       桌面端折叠/展开
  toggleMobileDrawer()  移动端抽屉
  toggleTheme()         切换深色/浅色（立即改 DOM + 持久化）
  setCurrentModelId()   切换模型
```

#### chat store — 聊天核心

```js
数据结构:
  history[]             会话列表 [{ id, title, updatedAt }]
  messagesByChatId      每个会话的消息 { [chatId]: [{ role, content }] }
  currentChatId         当前活跃会话
  undoState             5 秒撤销数据
  isRegenerating        是否正在重新生成 AI 回复

核心操作:
  addMessage(role, content)
    → 无 currentChatId 时自动创建会话
    → 追加消息到数组
    → 自动根据首条消息生成标题
    → 淘汰超过 50 个的旧会话 / 200 条的旧消息

  appendToLastMessage(content)
    → SSE 流式拼接，每到一个 chunk 调用一次

  deleteTurnByUserIndex() / deleteTurnByAssistantIndex()
    → 删除一组对话（user + assistant）
    → 保存到 undoState，5 秒内可用 undoDelete() 恢复

  regenerateReply()
    → 保留用户消息，重新生成 AI 回复
    → 独立 AbortController

  buildTitleFromContent()
    → "请帮我写一个Vue3的响应式原理解析。可以吗？"
    → 去掉"请帮我" → 截第一个标点 → "Vue3的响应式原理解析"（最长 20 字）
```

#### interview store — 面试状态机

```js
状态流转:
  idle → config(选预设) → answering(答题) → scoring(AI 评分中)
       → feedback(看结果) → answering(追问) → ... → finished(全部完成)

数据结构:
  questions[]         本轮所有题目
  currentIndex        当前题号
  phase               当前阶段
  answers{}           { [questionId]: '用户答案' }
  scores{}            { [questionId]: { score, correctness, completeness, clarity, feedback, improvedAnswer } }
  conversations{}     { [questionId]: [{ role, content }] }  // 追问对话记录
  history[]           每次完成的面试完整存档

计算属性:
  knowledgePointStats  每个知识点的平均得分 → 用于雷达图
  categoryStats        每个分类的平均得分
  weakPoints           得分 < 5 的知识点列表
  overallStats         历史总统计（总次数、平均分、最近 10 次趋势）

操作:
  startInterview(type)       → 题库出题：selectQuestions() 选题
  loadCustomQuestions(list)  → 文件/AI 出题：加载外部题目
  submitAnswer(id, answer)   → 提交答案，进入 scoring
  handleEvaluateResult()     → 处理追问结果：follow_up / complete
  finishInterview()          → 归档到 history
  exportRecords()            → 调用 interviewExport.js 导出
```

#### knowledge store — 知识库状态

```js
状态:
  kbs[]         知识库列表（摘要）
  currentKB     当前打开的详情（含 files 数组）
  loading       加载中标记
  error         错误信息

操作:
  fetchKBs() → fetchKnowledgeBases() → kbs.value = ...
  createKB(name) → createKnowledgeBase() → kbs.unshift()
  fetchKB(id) → fetchKnowledgeBase(id) → currentKB.value = ...
  uploadFile(kbId, { name, type, content }) → 更新 currentKB.files
  deleteFile(kbId, fileId) → 更新 currentKB.files
  generateQuestions(kbId, opts) → generateFromKB() → 返回题目数组
```

### 7.4 API 层 `src/utils/`

#### apiClient.js — 统一请求封装

```js
async function apiRequest(url, options = {}) {
  const res = await fetch(url, {
    headers: { 'Content-Type': 'application/json', ...options.headers },
    ...options
  })
  if (!res.ok) {
    const data = await res.json().catch(() => ({}))
    throw new Error(data.error || `请求失败: ${res.status}`)
  }
  return res.json()
}
```

所有非流式请求都走这个函数。`interviewApi.js` 和 `knowledgeApi.js` 都是对它的薄封装。

#### chatApi.js — SSE 流式请求

```js
function requestChatStream(model, messages, { onChunk, onDone, onError, signal }) {
  fetch('/api/chat', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ model, messages }),
    signal  // ← AbortController，用于取消
  })
  .then(res => {
    const reader = res.body.getReader()
    const decoder = new TextDecoder()
    // 循环读取 SSE 流
    function read() {
      reader.read().then(({ done, value }) => {
        if (done) { onDone(); return }
        const text = decoder.decode(value)
        // 解析 SSE 格式: data: {"content":"..."}\n\n
        // 每解析出一个 content chunk → onChunk(content)
        read()  // 递归继续
      })
    }
    read()
  })
  .catch(err => {
    if (err.name === 'AbortError') return  // 用户主动取消，不算错误
    onError(err)
  })
}
```

**关键设计**：
- `AbortController` 同时用于发送途中的正常取消和用户的主动停止
- 区分 `AbortError`（正常）和其他网络错误
- 解析 `data: [DONE]` 作为结束信号

#### docParser.js — 客户端文件解析

```
PDF  → pdfjs-dist (懒加载 import) → 逐页提取文本 → 拼接
Word → mammoth → HTML → 提取纯文本
TXT  → FileReader.readAsText → 直接读
```

**关键**：文件解析在前端完成，只把纯文本传给后端。因为 pdfjs-dist 和 mammoth 体积大（~2MB），用懒加载避免影响首屏。

### 7.5 虚拟滚动 `useVirtualScrollHeight.js`

**为什么需要**：聊天消息可能几百条，每条包含 Markdown 渲染和代码高亮，全量渲染 DOM 会卡死。

**方案**：`vue3-virtual-scroller` 的 `<RecycleScroller>` — 只渲染可视区域内的消息。

**高度管理**：
```
1. sizeMap = new Map()  // 非响应式！防止触发全量重算
2. 每条消息挂载时 → new ResizeObserver(entry) → 记录实际高度
3. schedule() → requestAnimationFrame + 160ms 节流
4. flush() → 直接 mutate 数组元素的 .size 属性
   └─ 不创建新数组！保持引用稳定，防止 RecycleScroller 全量重建
5. estimateMessageHeight() → 中英文区分预估高度
   └─ 中文: 约 40 字/行 → 每 20 token 一行
   └─ 英文: 约 80 字/行 → 每 5 token 一行
   └─ 图片/代码块额外加固定高度
```

### 7.6 语音输入 `useSpeechRecognition.js`

```js
init()
  → new (window.SpeechRecognition || webkitSpeechRecognition)()
  → lang: 'zh-CN'
  → continuous: true       // 持续识别
  → interimResults: false  // 只要最终结果

toggle()
  → isRecording ? stop() : start()

回调:
  onresult → 拿到 transcript → 插入输入框
  onerror  → 静默失败
  onend    → 自动重启（continuous 模式下）
```

### 7.7 文件解析 `docParser.js`

三种文件类型的解析链路：

```
PDF:
  import('pdfjs-dist') → getDocument(data) → pdf.getPage(i) → page.getTextContent()
  → items.map(i => i.str).join(' ') → 返回纯文本

Word (.docx):
  import('mammoth') → extractRawText({ arrayBuffer })
  → result.value → 返回纯文本

TXT:
  new Promise((resolve) => {
    const reader = new FileReader()
    reader.onload → resolve(reader.result)
    reader.readAsText(file)
  })
```

**懒加载**：pdfjs-dist 和 mammoth 只在需要时才 `import()`，不影响首屏加载。

---

## 八、四种 AI 交互模式深度剖析

### 模式 1：SSE 流式聊天

```
场景: 用户在 ChatView 输入框发送消息

前端:
  chatStore.addMessage('user', content)
  → ChatInput 创建 AbortController
  → requestChatStream(model, messages, { onChunk, onDone, onError, signal })
  → onChunk → chatStore.appendToLastMessage(chunk)
  → onDone  → ChatInput 恢复为发送按钮

后端:
  POST /api/chat → writeSSEHeaders → streamChat(model, messages)
  → AsyncGenerator 逐 token yield
  → 每个 token: data: {"content":"x"}\n\n
  → 结束: data: [DONE]\n\n

中断:
  点击停止按钮 → abortController.abort()
  → fetch 抛 AbortError → chatApi 静默忽略
```

### 模式 2：单次 JSON 评分

```
场景: 普通模式下一题答完，点击提交

前端:
  interviewStore.submitAnswer(questionId, answer)
  → phase = 'scoring'
  → requestScore({ question, userAnswer, answerPoints })
  → interviewStore.saveScore(questionId, result)
  → phase = 'feedback'（展示评分详情）

后端:
  POST /api/interview/score
  → 拼 SCORE_PROMPT 模板
  → callAI({ temperature: 0.3, maxTokens: 600 })
  → 返回 { score, correctness, completeness, clarity, feedback, improvedAnswer }
```

### 模式 3：多轮追问评估（当前是「伪 Agent」）

```
场景: 深度模式下一题答完

前端:
  interviewStore.submitAnswer(questionId, answer, useDeepMode=true)
  → appendToConversation(questionId, 'user', answer)
  → POST /api/interview/evaluate

后端:
  → 拼 EVALUATE_PROMPT（含对话历史）
  → callAI → 返回 { action }:

  如果 action = 'follow_up':
    → interviewStore.handleEvaluateResult()
    → appendToConversation('assistant', followUpQuestion)
    → phase = 'answering'（展示追问，等待用户回答）
    → 用户回答后再次 POST /api/interview/evaluate

  如果 action = 'complete'（或达到 maxRounds=3）:
    → interviewStore.saveScore(questionId, result)
    → phase = 'feedback'

追问上限:
  MAX_ROUNDS = 3 ← 这是硬编码的，Agent 改造后由 AI 自主决定
```

### 模式 4：AI 生成面试题

```
三种入口:

1. 文件出题:
   docParser.js 解析文件 → POST /api/questions/generate
   → content 超过 8000 字符硬截断 → callAI → 返回 JSON 数组

2. 岗位出题:
   POST /api/questions/generate-by-role
   → 不需要 content → callAI 用自身知识出题

3. 知识库出题:
   POST /api/knowledge/:id/generate
   → 聚合 KB 所有 txt → 超过 10000 字符硬截断 → callAI

三种的共同后处理:
   → 补全 id / type / category / tags / knowledgePoints 字段
   → 返回 { questions: [...] }
   → interviewStore.loadCustomQuestions(questions)
```

---

## 九、面试系统

### 题库结构

```js
// src/data/questions/vue.js 示例
export default [
  {
    id: 'vue-001',
    category: 'vue',
    difficulty: 'medium',
    knowledgePoints: ['响应式原理', 'Proxy'],
    question: 'Vue 3 的响应式原理是什么？',
    answerPoints: ['基于 Proxy 实现', '...']
  }
]
```

### 分层抽样算法 `selectQuestions()`

```js
当 difficulty === 'all' 时（默认）:
  1. 按 easy / medium / hard 分组
  2. 按比例抽取: 40% easy + 40% medium + 20% hard
  3. 某难度不足 → 从其他难度随机补充
  4. 最终按 easy → medium → hard 排序返回

当 difficulty !== 'all' 时:
  1. 只取该难度的题 → 随机打乱 → 取前 N 道
```

### 4 种预设

| 预设 key | 标签 | 分类 | 默认题数 |
|----------|------|------|:--:|
| `frontend` | 前端综合面试 | 全部 6 类 | 10 |
| `js-core` | JS 核心 | JavaScript | 6 |
| `vue-special` | Vue 专项 | Vue | 5 |
| `css-html` | HTML + CSS | HTML/CSS | 6 |

### 面试历史数据结构

```js
// interviewStore.history[] 每条记录:
{
  id: '1700000000000',
  type: 'frontend',
  typeLabel: '前端综合面试',
  questions: [...],         // 本轮所有题目
  answers: { [qId]: '...' },
  scores: { [qId]: { score, correctness, completeness, clarity, feedback, improvedAnswer } },
  conversations: { [qId]: [{ role, content }] },
  isCustomMode: false,
  customSource: '',
  startedAt: '2024-...',
  finishedAt: '2024-...',
  totalScore: 7.5
}
```

### 统计计算

```js
knowledgePointStats  → 每个知识点的平均得分 → 雷达图数据
categoryStats        → 每个分类的平均得分 → 柱状图数据
weakPoints           → 得分 < 5 的知识点 → 薄弱项提醒
overallStats         → 总次数、平均分、最近 10 次趋势

评分颜色:
  ≥ 8: emerald 绿
  5-7: amber 橙
  < 5: red 红
```

---

## 十、知识库系统

### 文件存储（无数据库）

```
server/data/knowledge/
├── index.json
│   [
│     {
│       "id": "kb-1700000000000",
│       "name": "Vue 面试资料",
│       "description": "...",
│       "fileCount": 3,
│       "createdAt": "2024-..."
│     }
│   ]
│
└── kb-1700000000000/
    ├── meta.json
    │   {
    │     "id": "kb-1700000000000",
    │     "name": "Vue 面试资料",
    │     "files": [
    │       {
    │         "id": "f-1700000000001",
    │         "name": "vue3-reactive.pdf",
    │         "type": "pdf",
    │         "charCount": 4523,
    │         "uploadedAt": "2024-..."
    │       }
    │     ]
    │   }
    │
    └── files/
        ├── f-1700000000001.txt    ← 纯文本内容（前端解析后上传）
        └── f-1700000000002.txt
```

### 完整上传流程

```
用户在前端选择一个 PDF 文件
  → docParser.js 懒加载 pdfjs-dist
  → 逐页提取文本 → 拼接为纯文本字符串
  → knowledgeStore.uploadFile(kbId, { name, type: 'pdf', content: '纯文本' })
  → POST /api/knowledge/:id/files { name: 'vue3-reactive.pdf', type: 'pdf', content: '...' }
  → 后端: sanitizeString(content, maxLength=100000) → 写入 .txt
  → 更新 meta.json 的 files 数组 → 更新 index.json 的 fileCount
```

### 出题时的问题

```
POST /api/knowledge/:id/generate
  → 遍历 meta.files → readFileContent() 逐个读 .txt
  → combined = 所有文件内容拼接
  → if (combined.length > 10000) → combined = combined.slice(0, 10000)
     ↑ 后面的内容丢失！大知识库出题质量低
```

---

## 十一、主题与 UI 系统

### 双层设计

```
tailwind.config.js                         theme.css
───────────────────────────────────        ───────────────────────
colors: {                                  :root {
  background: 'var(--color-background)'      --color-background: #ffffff
  primary:    'var(--color-primary)'         --color-primary:    #3b82f6
  surface:    'var(--color-surface)'         --color-surface:    #f8fafc
  text:       'var(--color-text)'            --color-text:       #1e293b
  border:     'var(--color-border)'          --color-border:     #e2e8f0
}                                          }
                                           .dark {
                                             --color-background: #0f172a
                                             --color-primary:    #60a5fa
                                             // ...
                                           }
```

**切换主题**：`document.documentElement.classList.toggle('dark')` → 所有变量同时切换 → 所有使用这些 utility class 的元素自动变色。

### 评分颜色系统

```js
// interviewHelpers.js — 全局统一
getScoreColor(score)   // ≥8: 'text-emerald-400' | ≥5: 'text-amber-400' | 'text-red-400'
getScoreBg(score)      // ≥8: 'bg-emerald-500/10' | ...
getScoreBorder(score)  // ≥8: 'border-emerald-500/30' | ...
getScoreLabel(score)   // ≥8: '优秀' | ≥5: '良好' | '需加强'
```

所有组件都使用这套映射，不各自写颜色判断。

### 启动防闪烁

```js
// main.js — Vue 挂载前就执行
try {
  const raw = localStorage.getItem('app')
  if (raw) {
    const app = JSON.parse(raw)
    if (app.isDark === false) document.documentElement.classList.remove('dark')
    else document.documentElement.classList.add('dark')
  } else {
    document.documentElement.classList.add('dark')  // 默认深色
  }
} catch (_) {
  document.documentElement.classList.add('dark')
}
// 然后才 createApp().mount()
```

---

## 十二、关键设计决策

| 决策 | 原因 |
|------|------|
| **Pinia 全量持久化** | 用户刷新后不掉会话、不掉面试进度。简单粗暴但有效 |
| **AI 调用两条路径分离** | 流式走 `streamChat`（AsyncGenerator），非流式走 `callAI`（Promise）。职责明确 |
| **extractJson 独立** | AI 返回经常带 markdown 包裹或解释文字，不能假设原生 JSON |
| **模型白名单** | 只有 `deepseek-v4-flash` 和 `deepseek-v4-pro`。非法值静默回退，不报错 |
| **文件解析在前端** | pdfjs-dist 和 mammoth 体积大（~2MB），放前端避免服务器压力。懒加载不影响首屏 |
| **知识库用文件系统** | 校招项目不需要数据库。文件系统够用，维护简单 |
| **虚拟滚动用 RecycleScroller** | DOM 复用比渲染全部消息快十几倍。高度映射用非响应式 Map + mutate 原地更新 |
| **SSE 防缓冲填充** | 2KB 注释填充确保 Nginx 和某些反向代理立即转发第一个字节 |
| **撤回用 5 秒 Timer** | 不是真正删除，是"标记待删除"。5 秒内可撤销，体验好 |

---

## 十三、当前局限与演进方向

### 四个明确痛点

| # | 问题 | 位置 | 影响 | RAG/Agent 解法 |
|---|------|------|------|---------------|
| 1 | **知识库内容硬截断** | `knowledge.js` L268: `MAX_CONTENT=10000`，超过直接 `slice` | 大文件后半截知识丢失，出题不全 | RAG: 分块 → Embedding → 向量检索，不截断按需取 |
| 2 | **追问轮次硬编码** | `interview.js` L116: `MAX_ROUNDS=3` | 3 轮不够时深度不足，1 轮够时浪费 token | Agent: AI 自主决定追问几轮、追问什么 |
| 3 | **出题无检索能力** | `questions.js` + `knowledge.js` 出题接口 | 全文塞 prompt，文件大就截断 | RAG: 先检索知识点再出题，覆盖面更全 |
| 4 | **无跨会话记忆** | 无对应实现 | 每次面试从零开始，不知道用户历史薄弱点 | 长期记忆: 向量库存面试历史摘要，下次针对性出题 |

### 演进路径

```
第一步: RAG 改造知识库
  新增: embedding.js / chunker.js / vectorStore.js / rag.js
  改动: knowledge.js 上传时触发入库，出题时先检索再生成

第二步: Agent 改造面试官
  新增: agent.mjs（Vercel AI SDK）
  改动: interview.js evaluate 接口接入 Agent 循环
         AI 自主决定追问/出题/评分

第三步: 长期记忆
  用同一套向量库存用户面试历史摘要
  下次面试时检索相关历史，个性化出题
```

---

## 附录：依赖全景

### 前端 `package.json`

| 包 | 版本 | 用途 |
|----|------|------|
| `vue` | 3.x | 框架 |
| `vue-router` | 4.x | 路由 |
| `pinia` | 2.x | 状态管理 |
| `pinia-plugin-persistedstate` | 4.x | Store → localStorage |
| `vue3-virtual-scroller` | 2.x | 虚拟滚动 |
| `chart.js` | 4.x | 统计图表 |
| `markdown-it` | 14.x | Markdown 渲染 |
| `highlight.js` | 11.x | 代码语法高亮 |
| `floating-vue` | 5.x | Tooltip/Dropdown |
| `pdfjs-dist` | 4.x | PDF 解析（懒加载） |
| `mammoth` | 1.x | Word 解析 |
| `@iconify/vue` | 4.x | 图标（Lucide） |

### 后端 `server/package.json`

| 包 | 版本 | 用途 |
|----|------|------|
| `express` | 4.x | HTTP 框架 |
| `openai` | 6.x | AI SDK（对接 DeepSeek） |
| `dotenv` | 16.x | 环境变量 |
| `helmet` | 8.x | 安全 HTTP 头 |
| `compression` | 1.x | Gzip 压缩 |
| `cors` | 2.x | 跨域 |
| `express-rate-limit` | 8.x | API 限流 |

---

> **这份文档的目标**：新来的开发者读完这篇，应该能理解项目的每一层在做什么，为什么这样做，以及哪里需要改进。
> 配合 `RAG-Agent学习指南.md` 一起看，就能从「理解现有项目」过渡到「规划下一步怎么改」。
