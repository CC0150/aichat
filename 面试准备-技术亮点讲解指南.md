# Intervy（AI 面试训练平台）— 面试讲解指南

> 帮助你系统梳理项目技术亮点，每个代码块都附带**逐行讲解**，理解"怎么写"和"为什么这样写"。

---

## 目录

1. [项目概述](#1-项目概述30-秒电梯演讲)
2. [技术架构总览](#2-技术架构总览)
3. [亮点一：虚拟滚动性能优化](#3-亮点一虚拟滚动性能优化)
4. [亮点二：SSE 流式对话架构](#4-亮点二sse-流式对话架构)
5. [亮点三：智能面试引擎](#5-亮点三智能面试引擎)
6. [亮点四：知识库系统](#6-亮点四知识库系统)
7. [亮点五：工程化细节](#7-亮点五工程化细节)
8. [常见追问 & 应答策略](#8-常见追问--应答策略)
9. [面试模拟：串讲路线图](#9-面试模拟串讲路线图)
10. [附录：关键词中英对照表](#附录关键词中英对照表)

---

## 1. 项目概述（30 秒电梯演讲）

> **一句话**：基于 Vue 3 + Express + DeepSeek API 的全栈 AI 面试训练平台，支持 SSE 流式对话、AI 多轮追问评分、三种出题模式，在性能和工程化上做了深度优化。

> **展开版（1 分钟）**：这是一个面向前端开发者的 AI 面试模拟平台。前端 Vue 3 + Pinia + Tailwind，后端 Express 做 API 代理转发到 DeepSeek。核心功能三块：自由 AI 技术对话（SSE 流式）、结构化面试模拟（本地题库 + 文件 + 知识库三种出题方式 + AI 多轮追问评分）、知识库管理（上传 PDF/Word 自动解析）。性能方面做了虚拟滚动的深度优化——ResizeObserver 动态测高 + 160ms 节流 + RAF 批量更新，用非响应式 Map 和原地 mutate 避免了流式输出时的全量重排。工程化上处理了 Safari 隐私模式、暗色模式防闪烁、LRU 淘汰、5 秒可撤销删除等细节。

---

## 2. 技术架构总览

```
┌──────────────────────────────────────────────────────────┐
│                     Frontend (Vite)                       │
│  Vue 3 + Pinia + Vue Router + Tailwind + FloatingVue     │
│                                                          │
│  Components: ChatView / InterviewView / KnowledgeView    │
│  Stores: app / chat / interview / knowledge (persisted)  │
│  Utils: chatApi (SSE) / interviewApi / knowledgeApi      │
│         docParser (PDF/Word) / tokenCounter / ...        │
│         useVirtualScrollHeight / useSpeechRecognition    │
└──────────────────────┬───────────────────────────────────┘
                       │  /api/*
                       ▼
┌──────────────────────────────────────────────────────────┐
│                   Backend (Express)                       │
│  Middleware: compression → helmet → cors → json → ratelimit│
│  Routes: /api/chat → SSE streaming                       │
│          /api/interview → score / evaluate                │
│          /api/questions → generate (file / role)          │
│          /api/knowledge → CRUD + generate questions       │
│  Services: deepseek.js (stream) / aiCompletions.js (json)│
│  Storage:  file-system knowledge bases                    │
└──────────────────────┬───────────────────────────────────┘
                       │  OpenAI SDK (openai v6)
                       ▼
┌──────────────────────────────────────────────────────────┐
│                 DeepSeek API (v4-flash / v4-pro)          │
└──────────────────────────────────────────────────────────┘
```

**关键设计决策**：
- 后端是**薄代理层**——不做业务逻辑，只做 API 转发、参数校验、输入消毒
- 所有 AI 调用走两条通路：`deepseek.js`（SSE 流式）、`aiCompletions.js`（JSON 非流式）
- 前端所有 Pinia store 通过 `pinia-plugin-persistedstate` 自动持久化到 localStorage
- Vite 开发时 proxy `/api` 到 `localhost:3001`

---

## 3. 亮点一：虚拟滚动性能优化

### 3.1 一句话概括

> 基于 `vue3-virtual-scroller` 的 `DynamicScroller` 封装自适应高度方案，通过 `shallowRef` + `ResizeObserver` + 160ms 节流 + RAF 批量更新 + 非响应式 `Map` + 原地 mutate，避免流式输出时的全量重排。

### 3.2 问题背景

AI 聊天场景的特点：消息数量多（一个会话几百条）、每条高度差异大（一行到几百行代码块）、**流式输出时内容持续变化导致高度不断增长**。如果全部渲染到 DOM，性能会崩溃。

---

### 3.3 第一层：shallowRef 数据层

> **文件**：`src/components/MessageArea.vue`

```js
// ★ 核心：用 shallowRef 而不是 ref 持有消息数组
// shallowRef 只追踪 .value 的引用变化（= 新数组），
// 不追踪数组内部元素的属性变化
const virtualMessages = shallowRef([])

function syncVirtualMessages() {
  virtualMessages.value = chatStore.currentMessages.map((m, i) => ensureMessageId(m, i))
}
syncVirtualMessages()

// 仅在结构变化时（消息增删、切换会话）重建整个数组
watch(() => chatStore.currentMessages.length, () => syncVirtualMessages())
watch(() => chatStore.currentChatId,       () => syncVirtualMessages())
```

**逐行讲解**：

| 代码 | 做了什么 | 为什么这样写 |
|------|---------|-------------|
| `shallowRef([])` | 创建一个只追踪引用变化的响应式数组 | 流式输出每秒有 20-50 次 `item.content` 变化，如果用 `ref`，每次变化 Vue 都会深度遍历整个数组做依赖追踪。`shallowRef` 只看 `.value` 是否被替换为新数组，不管数组内部元素的属性变化 |
| `ensureMessageId(m, i)` | 给每条消息补一个稳定的 `id` | DynamicScroller 用 `key-field="id"` 来识别 item，id 必须稳定，否则每次流式更新都会销毁并重建 DOM |
| `syncVirtualMessages()` | 用 `.map()` 生成新数组再赋值 | 这样 `.value` 引用变了，shallowRef 才触发，DynamicScroller 才感知到 item 增删 |
| `watch(...length)` | 消息数量变化时重建数组 | 只在真正有增删时重建，流式输出时 content 变但 length 不变 → 不触发 |
| `watch(...currentChatId)` | 切换会话时重建数组 | 不同会话的消息是全新的，必须重建 |

**模板中使用**：

```html
<DynamicScroller
  ref="scrollerRef"
  :items="virtualMessages"
  :min-item-size="120"
  key-field="id"
  style="overflow-anchor: none"
>
  <template #default="{ item, index, active }">
    <DynamicScrollerItem :item="item" :index="index" :active="active">
      <!-- 消息气泡渲染 -->
    </DynamicScrollerItem>
  </template>
</DynamicScroller>
```

**讲解要点**：

| 属性 | 作用 | 为什么 |
|------|------|--------|
| `:min-item-size="120"` | 告诉 DynamicScroller 每条消息至少 120px 高 | 帮助它计算初始的虚拟视口范围，避免全部渲染 |
| `key-field="id"` | 动态高度的 item 必须有唯一 key | DynamicScroller 用它来追踪每个 item 的高度缓存，id 不变就不会重置高度 |
| `overflow-anchor: none` | 禁用 Chrome 的 Scroll Anchoring | Chrome 默认会在 DOM 插入时自动调整滚动位置保持"视觉稳定"，但流式输出时我们需要自己控制滚动——开着会打架 |

---

### 3.4 第二层~第四层：ResizeObserver + 节流 + 批量更新

> **文件**：`src/composables/useVirtualScrollHeight.js`（完整实现，128 行）

```js
import { onUnmounted } from 'vue'

export function useVirtualScrollHeight(virtualMessagesRef) {
```

#### 3.4.1 数据结构初始化

```js
  // ===== 核心：非响应式 Map =====
  // ★ 普通 new Map()，不是 reactive(new Map())
  // Vue 响应式系统完全不追踪这里的读写 — 零开销
  const sizeMap = new Map()

  // ===== ResizeObserver 实例管理 =====
  const observers = new Map()       // id → ResizeObserver 实例

  // ===== 节流 + RAF 状态 =====
  let rafId = null                  // 当前是否有待执行的 RAF
  let throttleId = null             // 当前是否有待执行的 setTimeout
  let lastFlush = 0                 // 上次 flush 的时间戳（ms）
  const THROTTLE_MS = 160           // 节流窗口：160ms ≈ 10 帧
  const pending = new Map()         // 待更新：id → height（覆盖式）
```

| 变量 | 为什么这样设计 |
|------|---------------|
| `sizeMap = new Map()` | **不用 `reactive(new Map())`** 是因为 Vue 3 对 Map 的 Proxy 封装有额外开销——每次 `set` 都会走 Proxy 拦截 → 触发依赖收集。在高频写入场景（每秒几十次）这是不必要的性能浪费。普通 Map 零开销 |
| `observers = new Map()` | 每个消息 DOM 元素一个独立的 ResizeObserver，用 Map 管理生命周期——挂载时创建、卸载时 disconnect |
| `THROTTLE_MS = 160` | 约等于 60fps 下 10 帧的时间窗口。足够合并多次 ResizeObserver 回调，同时用户感知不到延迟 |
| `pending = new Map()` | 用 Map 而不是 Object，因为 key 是消息 id（任意字符串），且 `id → height` 的覆盖语义天然适合 Map |

#### 3.4.2 flush() — RAF 回调中的批量写入

```js
  function flush() {
    const msgs = virtualMessagesRef?.value
    pending.forEach((h, id) => {
      // ① 写入非响应式 Map → DynamicScroller 通过 getSize(id) 查询
      sizeMap.set(id, h)
      if (msgs) {
        // ② ★ 关键操作：原地 mutate
        //    直接在已有数组元素上修改 size 属性
        //    不替换整个数组，shallowRef 不触发
        const item = msgs.find((m) => m.id === id)
        if (item) item.size = h
      }
    })
    pending.clear()
    rafId = null
  }
```

**逐行讲解**：

| 步骤 | 代码 | 解释 |
|------|------|------|
| ① | `sizeMap.set(id, h)` | 更新非响应式 Map。DynamicScroller 内部会调用 `getSize(id)` 来查这个 Map，获取最新的高度 |
| ② | `item.size = h` | **这是整个方案的灵魂**。直接修改数组元素的 `size` 属性，不是 `virtualMessages.value = newArray`。因为 `shallowRef` 只看引用是否变了，引用没变就不触发。但 DynamicScroller 读的是 `item.size` 这个属性值，它拿到了新高度就正确渲染了 |
| 整体 | `forEach + clear` | 一次同步遍历处理所有待更新，然后清空。在 RAF 回调中执行，保证在浏览器下一帧绘制前完成——用户看不到中间状态 |

**面试中如何解释 `item.size = h` 这一步**：
> "正常情况下你改数组元素的属性，Vue 会检测到变化并触发重渲染。但这里我用了 `shallowRef`——它不追踪数组内部元素的属性变化。所以 `item.size = h` 这个操作 Vue 响应式系统完全不感知，不会触发任何重渲染。但是 DynamicScroller 它读取的是 `item.size` 这个值，它拿到了新高度就能正确计算每个 item 的位置。这就是原地 mutate 的精髓——绕过了 Vue 的响应式系统，直接把高度信息传递给虚拟滚动组件。"

#### 3.4.3 schedule() — 160ms 节流调度

```js
  function schedule(id, height) {
    // ① 覆盖同一 id 的旧值 — pending 只保留每个 id 的最新高度
    pending.set(id, Math.round(height))

    // ② 如果已经有待执行的 RAF，只需更新 pending 值即可
    //    等上次 schedule 触发的 flush 执行时会拿到最新的 height
    if (rafId) return

    // ③ 计算距离上次 flush 的时间差
    const remaining = THROTTLE_MS - (performance.now() - lastFlush)
    const doFlush = () => {
      lastFlush = performance.now()
      flush()
    }

    if (remaining <= 0) {
      // ④ 距上次刷新已超过 160ms → 立即 RAF
      rafId = requestAnimationFrame(doFlush)
    } else {
      // ⑤ 距上次刷新不到 160ms → 等剩余时间后再 RAF
      throttleId = setTimeout(() => {
        throttleId = null
        rafId = requestAnimationFrame(doFlush)
      }, remaining)
    }
  }
```

**逐行讲解**：

| 步骤 | 代码 | 解释 |
|------|------|------|
| ① | `pending.set(id, Math.round(height))` | 覆盖写入。如果同一个 id 在节流窗口内被多次触发（流式输出时很常见），pending 只保留最新值 |
| ② | `if (rafId) return` | 早退优化。如果上次 schedule 已经排了一个 RAF 还没执行，这次只需要更新 pending 值就直接返回。等那个 RAF 执行时会读到最新的 pending |
| ③ | `performance.now() - lastFlush` | 用高精度时间 API 计算时间差，比 `Date.now()` 精度更高（微秒 vs 毫秒） |
| ④ | `remaining <= 0` → 直接 RAF | 距上次刷新已超过 160ms，说明"窗口"已经过了，可以直接排一个新的 RAF |
| ⑤ | `remaining > 0` → setTimeout + RAF | 还没到 160ms，用 setTimeout 等剩余时间再排 RAF。这样做是精确的 throttle——保证两次 flush 之间至少间隔 160ms |

**面试中如何解释这个 throttle 逻辑**：
> "ResizeObserver 在流式输出时会非常频繁地触发——每个 token 都可能导致高度变化，每秒可能几十次回调。如果每次都直接更新 sizeMap 和 item.size，DynamicScroller 会不停重新计算布局。所以我在 schedule 里做了一个 160ms 的 throttle：把高度先存到 pending Map 里，然后检查距离上次 flush 是否已过 160ms。如果过了就直接 RAF，没过就用 setTimeout 等剩余时间再 RAF。RAF 回调里的 flush 会一次性批量处理所有 pending 的高度更新。"

#### 3.4.4 observeItem() — ResizeObserver 生命周期

```js
  function observeItem(el, id) {
    if (!el) {
      // ★ Vue :ref 回调在元素卸载时会传入 null
      //    此时自动断开 ResizeObserver，防止内存泄漏
      const existing = observers.get(id)
      if (existing) {
        existing.disconnect()
        observers.delete(id)
      }
      return
    }
    // 防重复：同一个 id 只创建一次 Observer
    if (observers.has(id)) return

    // ★ 每个 item 一个独立 ResizeObserver
    const ro = new ResizeObserver((entries) => {
      for (const entry of entries) {
        const h = entry.contentRect.height
        if (h > 0) {
          // ★ 额外计算 marginBottom
          //    DynamicScroller 不自动算 margin，需要手动加
          const mb = parseFloat(getComputedStyle(entry.target).marginBottom) || 0
          schedule(id, h + mb)
        }
      }
    })
    ro.observe(el)
    observers.set(id, ro)
  }
```

**逐行讲解**：

| 代码 | 解释 |
|------|------|
| `if (!el) { ... disconnect }` | 利用了 Vue 的 `:ref` 回调模式——元素挂载时 `el` 是 DOM 元素，卸载时 `el` 是 `null`。在收到 `null` 时自动断开 Observer，不需要手动 `onUnmounted` 逐个清理 |
| `if (observers.has(id)) return` | DynamicScroller 复用 DOM 节点时可能对同一个 id 重复调用 ref 回调，这个判断防止创建重复的 Observer |
| `new ResizeObserver(...)` | 每个 item 独立一个 Observer。也可以用一个 Observer 同时 observe 所有元素，但独立 Observer 的优点是：单个 item 的挂载/卸载不影响其他 item 的观察 |
| `entry.contentRect.height` | ResizeObserver 提供的精确像素高度，比 `el.offsetHeight` 更可靠——它包含了 CSS transform 后的实际渲染高度 |
| `parseFloat(getComputedStyle(...).marginBottom)` | DynamicScroller 用它自己的高度模型计算 item 位置，它不自动包含 margin。所以我们需要手动读取 marginBottom 加到高度里 |
| `schedule(id, h + mb)` | 不直接更新，而是走 schedule → throttle → flush 管道 |

#### 3.4.5 高度估算函数 — 初始值

```js
export function estimateMessageHeight(msg, opts = {}) {
  const { charsPerLine, lineHeightPx, overheadPx, imagePx, minHeight } = {
    charsPerLine: 80,    // 假设每行约 80 个字符
    lineHeightPx: 20,    // 假设行高约 20px
    overheadPx: 90,      // 头像 + padding + 间距 约 90px
    imagePx: 120,        // 每张图片约 120px
    minHeight: 50,
    ...opts,
  }
  const text = getText(msg)
  const textLines = Math.max(1, Math.ceil(text.length / charsPerLine))
  // 图片只在用户消息中存在
  const imageCount =
    msg?.role === 'user' && msg?.content && typeof msg.content === 'object'
      ? msg.content.images?.length || 0 : 0
  return Math.max(minHeight,
    Math.round(overheadPx + textLines * lineHeightPx + imageCount * imagePx))
}
```

**讲解**：
> "在 ResizeObserver 测量到真实高度之前，DynamicScroller 需要一个初始的估算值。我根据字符数和行高做了一个简单估算：文本字符数 ÷ 80（每行约 80 个字符）× 20px 行高 + 90px 头像/padding 开销 + 120px × 图片数。这个值不需要精确——一旦 DOM 渲染出来，ResizeObserver 就会立即测量真实高度并更新。"

---

### 3.5 四层架构总结

```
ResizeObserver 检测到高度变化
      │
      ▼
schedule(id, height)          ← 存入 pending Map（覆盖同 id 旧值）
      │
      ├─ 距上次 flush < 160ms？→ setTimeout 等剩余时间 → RAF
      └─ 距上次 flush ≥ 160ms？→ 直接 RAF
                │
                ▼
flush()  [在 RAF 回调中执行]
      │
      ├─ sizeMap.set(id, h)    ← 非响应式 Map，零开销
      └─ item.size = h         ← 原地 mutate，不触发 shallowRef
```

**面试中画这个图**：

```
                   Vue 响应式系统（不追踪）
                         │
┌──────────┐      ┌─────┴──────┐      ┌──────────────┐
│ 组件渲染  │ ◄─── │ shallowRef  │      │   sizeMap    │
│(Dynamic  │      │ (messages)  │      │ (普通 Map)   │
│ Scroller)│      └─────────────┘      └──────▲───────┘
└────┬─────┘                                  │
     │ :ref 回调挂载/卸载                       │ 写入
     ▼                                        │
┌──────────────┐    height    ┌───────────────┴──┐
│ResizeObserver│ ──────────► │ schedule/flush    │
│  (per item)  │              │ 160ms + RAF      │
└──────────────┘              └──────────────────┘
```

**讲解话术**：**"数据流是单向的——ResizeObserver 检测 DOM 高度变化 → 走 160ms 节流 + RAF 批量处理 → 同时更新非响应式 Map（给 DynamicScroller 查询）和原地修改数组元素的 size 字段。Vue 的响应式系统完全不参与这个流程，所以不会造成额外的重渲染。DynamicScroller 读的是 item.size 的值和 sizeMap 的值，它拿到了正确的高度就能正确布局。"**

---

## 4. 亮点二：SSE 流式对话架构

### 4.1 一句话概括

> 服务端用 `async function*` generator 封装 OpenAI SDK 流式响应，2048 字节防缓冲填充确保流即时推送；前端手动消费 `ReadableStream` 逐 chunk 解析 SSE 协议；双 AbortController 协调发送/重新生成/继续生成三种中断恢复场景。

---

### 4.2 服务端：Async Generator + SSE 路由

> **文件**：`server/services/deepseek.js`

```js
const { openai } = require("../config");

/**
 * ★ async function* — 这是一个 Async Generator
 *    它不是一个普通函数，调用它返回一个 AsyncGenerator 对象
 *    可以用 for await...of 来消费它产出的值
 */
async function* streamChat(model, messages) {
  // ① 调用 OpenAI SDK（兼容 DeepSeek API），开启 stream 模式
  const stream = await openai.chat.completions.create({
    model,
    messages,
    stream: true,      // ← 关键参数：告诉 API 逐 token 返回
  });

  // ② for await...of 消费上游的流
  //    每次迭代拿到一个 chunk（OpenAI 的流式响应格式）
  for await (const chunk of stream) {
    // ③ 用可选链安全地取出 delta 中的 content
    //    流式响应中，每个 chunk 的 content 是增量（delta），不是完整文本
    const content = chunk.choices?.[0]?.delta?.content;
    // ④ 过滤掉空值和空字符串
    if (content != null && content !== "") {
      yield String(content);   // ← yield 暂停执行，等下游消费完再继续
    }
  }
}

module.exports = { streamChat };
```

**逐行讲解**：

| 步骤 | 代码 | 解释 |
|------|------|------|
| ① | `stream: true` | OpenAI 兼容 API 的流式模式开关。设为 true 后，API 不会一次性返回完整回答，而是逐个 token 增量返回 |
| ② | `for await (const chunk of stream)` | Async Generator 的标准消费方式。这是一个 **pull 模型**：你主动去拉取下一个值，而不是被动等数据推过来 |
| ③ | `chunk.choices?.[0]?.delta?.content` | 流式响应的 JSON 结构：`{ choices: [{ delta: { content: "token" } }] }`。用可选链防止任何一层为 null/undefined 时报错 |
| ④ | `yield String(content)` | **yield 是关键**。它暂停 generator 的执行，把当前 token 返回给消费者。消费者处理完后，generator 才继续执行取下一个 chunk。这就是**背压**机制——如果下游处理慢，generator 会自然等待，不会无限制地积压数据 |

**区别于 callback / EventEmitter**：
> "如果用 `on('data')` 回调模式，数据是 push 过来的，你无法控制接收速度。Async Generator 是 pull 模型——你主动 `for await` 拉取，执行权在你手里。而且 try-catch 可以自然地包裹整个 for await 循环，错误处理非常简单。"

> **文件**：`server/routes/chat.js`

```js
const { Router } = require("express");
const { writeSSEHeaders } = require("../middleware");
const { streamChat } = require("../services/deepseek");
const { DEFAULT_MODEL, sanitizeModel } = require("../config");

const router = Router();

router.post("/", async (req, res) => {
  // ① ★ 先写入 SSE 响应头 + 2048 字节防缓冲填充
  writeSSEHeaders(res);

  const { model: rawModel = DEFAULT_MODEL, messages = [] } = req.body || {};
  // ② ★ 安全校验：sanitizeModel 只允许白名单中的模型名
  const model = sanitizeModel(rawModel);

  if (!messages.length) {
    res.write(`data: ${JSON.stringify({ error: "messages 不能为空" })}\n\n`);
    return res.end();
  }

  try {
    // ③ for await...of 消费 async generator
    //    每次 yield 一个 token，这里就 res.write 一个 SSE 帧
    for await (const chunk of streamChat(model, messages)) {
      // ④ SSE 协议格式：data: {JSON}\n\n
      res.write(`data: ${JSON.stringify({ content: chunk })}\n\n`);
    }
    // ⑤ 流结束信号：客户端通过这个判断流已结束
    res.write("data: [DONE]\n\n");
    res.end();
  } catch (err) {
    console.error("[chat] 接口报错:", err.message);
    // ⑥ 异常时的错误帧，同样用 SSE 格式发送
    res.write(
      `data: ${JSON.stringify({ error: err?.message || "未知错误" })}\n\n`
    );
    res.end();
  }
});

module.exports = router;
```

**逐行讲解**：

| 步骤 | 代码 | 解释 |
|------|------|------|
| ① | `writeSSEHeaders(res)` | 在写任何数据之前先设置响应头 + 2048B padding。如果先写了数据再设头，HTTP 协议不允许 |
| ② | `sanitizeModel(rawModel)` | 安全校验。用户不能随便传任意模型名（防注入），只能用白名单里的 `deepseek-v4-flash` 或 `deepseek-v4-pro` |
| ③ | `for await (const chunk of streamChat(...))` | generator 每 yield 一个 token，这里就拿到一个 chunk，然后立即 res.write 回客户端。这就是端到端的流式管道：DeepSeek → generator → res.write → 网络 → 浏览器 |
| ④ | `` `data: ${JSON.stringify(...)}\n\n` `` | SSE 协议规定每个事件以 `data:` 开头，以 `\n\n` 结尾。`\n\n` 是事件分隔符 |
| ⑤ | `"data: [DONE]\n\n"` | 自定义终止信号。客户端收到 `[DONE]` 就知道流结束，然后关闭 reader。注意 `[DONE]` 不是标准 SSE 协议，是我们自定义的协议约定 |
| ⑥ | 错误帧也是 SSE 格式 | 统一用 SSE 帧格式发送错误，客户端在同一个解析循环中处理 |

---

### 4.3 服务端：2048 字节防缓冲填充

> **文件**：`server/middleware/index.js`

```js
function writeSSEHeaders(res) {
  // ① ★ 用 writeHead 而不是 setHeader — writeHead 立即发送响应头
  res.writeHead(200, {
    "Content-Type": "text/event-stream; charset=utf-8",
    // ② 多级防缓存策略
    "Cache-Control": "no-cache, no-transform",
    Connection: "keep-alive",
    // ③ 针对 Nginx 的专用头
    "X-Accel-Buffering": "no",
  });
  // ④ ★ 2048 字节防缓冲填充
  //    SSE 协议中 ':' 开头的行为注释行，客户端会忽略
  //    但字节照常走网络传输，强制突破代理/Node.js 的缓冲区阈值
  res.write(": " + " ".repeat(2048) + "\n\n");
}
```

**逐行讲解**：

| 步骤 | 代码 | 解释 |
|------|------|------|
| ① | `res.writeHead(200, {...})` | 用 `writeHead` 而不是 `setHeader`，因为 SSE 需要立即发送头部——之后就开始写数据了。`setHeader` 配合 `status().json()` 的模式不适合流式响应 |
| ② | `Cache-Control: no-cache, no-transform` | `no-cache` 告诉浏览器/CDN 不要缓存。`no-transform` 告诉代理不要对响应做任何转换（如压缩成不同格式），因为这会破坏 SSE 的实时性 |
| ③ | `X-Accel-Buffering: no` | Nginx 特有的响应头。Nginx 默认会对代理的响应做缓冲——积累到一定大小再发给客户端。这个头告诉 Nginx 不要缓冲，每个 chunk 立即转发 |
| ④ | `": " + " ".repeat(2048)` | **核心技巧**。SSE 注释以 `:` 开头，客户端解析时会跳过。但 `2048 个空格 + \n\n` 作为 HTTP 响应的第一个数据块发送，2KB 远超大多数代理的缓冲区阈值（通常 512B-4KB），强制代理立即 flush 缓冲 |

**为什么是 2048 而不是 1448（MTU）？**
> "充分冗余。Nginx 默认 `proxy_buffer_size` 是 4KB 或 8KB（取决于版本），虽然通常只缓冲第一个数据块，但 2048 字节 + HTTP 头部（~500B）≈ 2.5KB，能确保突破大多数设置。实际上 >1500 字节就能覆盖常见 MTU，2048 是业界惯例。"

---

### 4.4 客户端：ReadableStream 手动消费 SSE

> **文件**：`src/utils/chatApi.js`（完整实现，103 行）

```js
export async function requestChatStream({ model, messages, onChunk, onError, signal }) {
  // ===== 第一步：发送 POST 请求 =====
  let response
  try {
    response = await fetch('/api/chat', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        Accept: 'text/event-stream; charset=utf-8',
      },
      body: JSON.stringify({ model, messages, stream: true }),
      signal,   // ← AbortSignal：用户点击 Stop 时，fetch 会抛出 AbortError
    })
  } catch (err) {
    if (err.name === 'AbortError') throw err   // 用户主动中止，往上抛给调用方处理
    const msg = `网络请求失败：${err.message || '无法连接到服务器'}`
    onError?.(msg)
    throw new Error(msg)
  }

  if (!response.ok) {
    // HTTP 错误（非 2xx）
    let msg = `API 请求失败：${response.status} ${response.statusText}`
    try {
      const text = await response.text()
      const data = text ? JSON.parse(text) : null
      const detail = data?.error?.message ?? data?.message ?? data?.error
      if (detail) msg += `（${detail}）`
    } catch (_) {}
    onError?.(msg)
    throw new Error(msg)
  }

  // ===== 第二步：获取 ReadableStream reader =====
  // ★ response.body 是一个 ReadableStream
  //    .getReader() 返回一个 ReadableStreamDefaultReader
  //    通过 reader.read() 手动拉取数据块
  const reader = response.body.getReader()
  const decoder = new TextDecoder()       // ★ 二进制 → 字符串解码器
  let buffer = ''                          // ★ 行缓冲：累积不完整的行

  // ===== 第三步：循环读取 =====
  while (true) {
    let done, value
    try {
      const result = await reader.read()   // 拉取下一个数据块
      done = result.done                   // true 表示流已关闭
      value = result.value                 // Uint8Array 二进制数据
    } catch (err) {
      if (err.name === 'AbortError') return // 用户中止
      throw err
    }
    if (done) break                        // 流正常结束

    // ===== 第四步：解码 =====
    // ★ { stream: true } 是关键参数
    //    告诉 TextDecoder 输入是分段的流
    //    如果某个多字节字符（如中文 "你" = 3 字节 UTF-8）被切在
    //    两个 chunk 之间，解码器会把不完整的字节保留在内部，
    //    等下一个 chunk 来了再拼接解码
    const chunkText = decoder.decode(value, { stream: true })
    buffer += chunkText

    // ===== 第五步：行缓冲 + 解析 SSE 协议 =====
    while (true) {
      const nl = buffer.indexOf('\n')      // 找第一个换行符
      if (nl === -1) break                 // 没有完整行 → 等下一个 chunk

      const rawLine = buffer.slice(0, nl)  // 取出一行
      buffer = buffer.slice(nl + 1)        // 从缓冲中移除这一行

      const line = rawLine.trim()
      if (!line.startsWith('data:')) continue  // 跳过非 data 行（注释、空行）

      const data = line.slice(5).trimStart()   // 去掉 "data:" 前缀

      // ===== 第六步：处理 [DONE] 信号 =====
      if (data === '[DONE]') {
        try { await reader.cancel() } catch (_) {}   // 主动关闭流
        return
      }

      // ===== 第七步：解析 JSON 并回调 =====
      try {
        const parsed = JSON.parse(data)
        if (parsed?.error) {
          onError?.(String(parsed.error))
          return
        }
        const content = parsed.content
                     ?? parsed.choices?.[0]?.delta?.content  // 兼容 OpenAI 原始格式
                     ?? null
        if (content != null && content !== '') {
          onChunk(String(content))   // ★ 回调上层，逐字符更新 UI
        }
      } catch (e) {
        console.debug('解析响应数据失败，忽略此行:', e)
      }
    }
  }
}
```

**逐段讲解**：

| 步骤 | 代码 | 做了什么 | 为什么这样写 |
|------|------|---------|-------------|
| ① | `fetch(..., { signal })` | 用 Fetch API 发送 POST，传入 AbortSignal | 用户点击 Stop 时 `controller.abort()` 会让 fetch 抛出 `AbortError`，实现网络层的中止 |
| ② | `response.body.getReader()` | 获取流式 reader | 不用 `response.text()` 或 `response.json()`——那些方法会等整个响应完成才返回。`getReader()` 可以逐块读取 |
| ③ | `await reader.read()` | 拉取下一个二进制数据块 | `{ done, value }` 中 `value` 是 `Uint8Array`，`done` 为 true 表示流结束 |
| ④ | `decoder.decode(value, { stream: true })` | 二进制 → 字符串，保持多字节字符完整性 | **不加 `stream: true` 的话**，如果 "你好" 的 "你" 的 3 个 UTF-8 字节被切在两个 chunk 之间（前一个 chunk 只包含前 2 个字节），直接 decode 会产出乱码 `��`。加上 `stream: true`，解码器会保留不完整字节等下一段 |
| ⑤ | 行缓冲 `buffer + indexOf('\n') + slice` | 累积不完整的行 | SSE 协议是行分隔的，但 TCP 是字节流——一个 `reader.read()` 可能返回半行或一行半。自己维护 buffer 逐行切分是标准做法 |
| ⑥ | `line.startsWith('data:')` | 过滤 SSE 帧 | SSE 协议中 `data:` 开头的行才包含数据。`:` 开头的是注释（如我们的 2048B padding），空行是事件分隔符 |
| ⑦ | `data === '[DONE]'` | 自定义终止协议 | 与服务器约定的信号。收到后主动 `reader.cancel()` 关闭底层 TCP 连接 |
| ⑧ | `onChunk(String(content))` | 回调上层 | 每个 token 调用一次回调，UI 层用 `chatStore.appendToLastMessage(content)` 逐字拼接显示 |

---

### 4.5 双 AbortController 协调

**三种操作，两个 Controller**：

```
┌────────────────────────────────────────────────────┐
│                    ChatInput 组件                    │
│  activeController (局部变量)                        │
│    ├── 发送消息 → new AbortController() → active    │
│    └── 继续生成 → new AbortController() → active    │
│                                                     │
│  abortCurrentRequest()                              │
│    ├── activeController.abort()                     │
│    └── chatStore.abortRegenerate()                  │
└────────────────────┬───────────────────────────────┘
                     │
                     ▼
┌────────────────────────────────────────────────────┐
│                   chatStore                         │
│  _regenerateAbort (非响应式变量)                     │
│    └── setRegenerateAbort(controller)               │
│    └── abortRegenerate()                            │
│                                                     │
│  isRegenerating (ref) — 供 isBusy 判断              │
│  lastInterruptedChatId — 供「继续生成」按钮         │
└────────────────────┬───────────────────────────────┘
                     │
                     ▼
┌────────────────────────────────────────────────────┐
│                  MessageArea 组件                    │
│  regenerate()                                       │
│    └── new AbortController()                        │
│    └── chatStore.setRegenerateAbort(controller)      │
└────────────────────────────────────────────────────┘
```

> **文件**：`src/components/ChatInput.vue`

```js
// ===== ChatInput 侧 — 管理「发送」和「继续生成」 =====
let activeController = null        // 普通变量，不需要响应式

// ★ 互斥判断：isSending 或 isRegenerating 任一为 true 都算忙
const isBusy = computed(() => isSending.value || chatStore.isRegenerating)

function abortCurrentRequest() {
  if (activeController) {
    try { activeController.abort() } catch (_) {}
    activeController = null
  }
  // ★ 同时中止 MessageArea 正在进行的 regenerate
  chatStore.abortRegenerate()
  isSending.value = false
  // ★ 标记被中断的会话 — 「继续生成」按钮通过这个判断是否显示
  if (chatStore.currentChatId) {
    const msgs = chatStore.currentMessages
    const last = msgs[msgs.length - 1]
    if (last && last.role === 'assistant' && String(last.content || '').trim()) {
      chatStore.lastInterruptedChatId = chatStore.currentChatId
    }
  }
}
```

> **文件**：`src/components/MessageArea.vue`

```js
// ===== MessageArea 侧 — 管理「重新生成」 =====
async function regenerate(index) {
  const userMessage = chatStore.currentMessages[index - 1]

  const controller = new AbortController()
  chatStore.setRegenerateAbort(controller)    // ← 存到 Store 供 ChatInput 访问
  chatStore.isRegenerating = true             // ← 让 isBusy 变为 true

  try {
    chatStore.setLastAssistantMessage('')     // ← 清空旧回复
    await requestChatStream({
      model: appStore.currentModel.model,
      messages: [{ role: 'user', content: userMessage.content }],
      onChunk: (content) => chatStore.appendToLastMessage(content),
      signal: controller.signal,             // ← 同一个 AbortSignal
    })
  } catch (error) {
    if (controller.signal.aborted || isAbortError(error)) return
    chatStore.setLastAssistantMessage(`Error: ${error.message}`)
  } finally {
    isGenerating.value = false
    chatStore.isRegenerating = false
    chatStore.setRegenerateAbort(null)
  }
}
```

> **文件**：`src/stores/chat.js`

```js
// ===== Store 侧 — 存储 regenerate 的 AbortController =====
let _regenerateAbort = null  // ★ 非 ref，不需要响应式

function setRegenerateAbort(controller) {
  _regenerateAbort = controller
}

function abortRegenerate() {
  if (_regenerateAbort) {
    try { _regenerateAbort.abort() } catch (_) {}
    _regenerateAbort = null
  }
  isRegenerating.value = false
}
```

**为什么要两个 Controller？**

| 问题 | 答案 |
|------|------|
| 为什么不合并成一个？ | 发送/继续由 ChatInput 组件管理，重新生成由 MessageArea 组件管理。各自管理生命周期——组件卸载时自动清理自己的 controller，不需要全局协调 |
| 怎么保证互斥？ | `isBusy = isSending \|\| isRegenerating`。用户点击 Stop 时，`abortCurrentRequest` 同时中止两个 controller |
| `_regenerateAbort` 为什么不是 ref？ | 这个值只被两个地方读写（set 和 abort），不需要响应式追踪。用普通变量避免不必要的响应式开销 |
| `lastInterruptedChatId` 的作用？ | "继续生成"按钮的显示条件：当前会话的最后一条消息是 assistant 角色，且 `lastInterruptedChatId === currentChatId` |

---

## 5. 亮点三：智能面试引擎

### 5.1 一句话概括

> 五态状态机驱动完整面试流程，本地题库按 40/40/20 难度分层随机抽样，AI 多轮追问上限 3 轮并通过 prompt 动态注入 `forceComplete` 硬约束。

---

### 5.2 五态状态机

> **文件**：`src/stores/interview.js`

```js
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import { selectQuestions, interviewTypes } from '@/data/questions/index.js'

export const useInterviewStore = defineStore('interview', () => {
  // ===== 五态定义 =====
  // ★ 整个面试流程只有这 5 个互斥状态
  //    任何时刻 phase 只能是其中之一，不存在"既在答题又在评分"
  const phase = ref('idle')    // idle | answering | scoring | feedback | finished

  const questions = ref([])     // 本轮题目列表
  const currentIndex = ref(0)   // 当前题目下标
  const answers = ref({})       // { [questionId]: userAnswer }
  const scores = ref({})        // { [questionId]: { score, correctness, completeness, clarity, ... } }
  const conversations = ref({}) // { [questionId]: [{ role:'user'|'assistant', content }] }

  // ★ 计算属性：当前题目从 questions[currentIndex] 取得
  const currentQuestion = computed(() => questions.value[currentIndex.value] || null)
  const isLastQuestion = computed(() => currentIndex.value >= questions.value.length - 1)
```

**状态转换函数**：

```js
  // ===== idle → answering =====
  function startInterview(typeKey, customCount, difficulty = 'all') {
    // ① 从题库按策略抽取题目
    const selected = selectQuestions({
      interviewType: typeKey,
      questionCount: customCount || interviewTypes[typeKey].questionCount,
      difficulty,
    })
    if (selected.length === 0) return false                   // 题目不够 → 拒绝开始
    questions.value = selected
    currentIndex.value = 0
    answers.value = {}; scores.value = {}; conversations.value = {}
    phase.value = 'answering'                                 // ← 状态切换
    return true
  }

  // ===== answering → scoring =====
  function submitAnswer(questionId, answer, useDeepMode = false) {
    answers.value[questionId] = answer                        // 保存答案
    if (useDeepMode) {
      appendToConversation(questionId, 'user', answer)        // 深度模式：记录对话历史
    }
    phase.value = 'scoring'                                   // ← 状态切换
  }

  // ===== scoring → feedback（普通模式） =====
  function saveScore(questionId, scoreData) {
    scores.value[questionId] = { ...scoreData }
    phase.value = 'feedback'                                  // ← 状态切换
  }

  // ===== scoring → answering（深度模式追问） =====
  function handleEvaluateResult(questionId, result) {
    if (result.action === 'follow_up') {
      // AI 返回追问 → 记录追问内容，回到 answering 让用户继续回答
      appendToConversation(questionId, 'assistant', result.followUpQuestion)
      phase.value = 'answering'                               // ← 回到 answering
      return 'follow_up'
    }
    // AI 返回 complete → 保存最终评分
    saveScore(questionId, { ...result })
    return 'complete'
  }

  // ===== feedback → answering（下一题）或 → finished（最后一题）=====
  function nextQuestion() {
    if (isLastQuestion.value) {
      finishInterview()
    } else {
      currentIndex.value++
      phase.value = 'answering'                               // ← 状态切换
    }
  }

  // ===== → finished =====
  function finishInterview() {
    phase.value = 'finished'                                  // ← 状态切换
    history.value.unshift({                                   // 存入历史
      id: Date.now().toString(),
      questions: [...questions.value], answers: { ...answers.value },
      scores: { ...scores.value }, totalScore: totalScore.value,
    })
  }

  // ===== finished → idle =====
  function resetInterview() {
    phase.value = 'idle'                                      // ← 状态切换
    questions.value = []; answers.value = {}; scores.value = {}
  }

  return {
    phase, currentQuestion, questions, currentIndex, scores, conversations,
    startInterview, submitAnswer, saveScore, handleEvaluateResult,
    nextQuestion, finishInterview, resetInterview,
  }
}, { persist: true })   // ★ Pinia 持久化：刷新页面恢复面试进度
```

**状态转换图**：

```
   ┌──────┐   startInterview()   ┌──────────┐
   │ idle │ ──────────────────► │ answering │ ◄───────────────┐
   └──────┘                     └─────┬─────┘                  │
      ▲                               │ submitAnswer()         │
      │                               ▼                        │
      │                          ┌─────────┐     follow_up     │
      │                          │ scoring │ ─────────────────┘
      │                          └────┬────┘   (deep mode only)
      │                               │ saveScore()
      │                               ▼
      │                          ┌──────────┐
      │         nextQuestion()   │ feedback │
      │  ◄────────────────────── │          │
      │                          └────┬─────┘
      │                               │ finishInterview()
      │                               ▼
      │                          ┌──────────┐
      └──────────────────────────│ finished │
            resetInterview()     └──────────┘
```

**讲解要点**：
1. 五个状态互斥——UI 渲染完全由 `phase` 决定，一个 `switch` 搞定所有视图
2. 深度模式多了一个 `scoring → answering` 的追问回路（最多 3 轮）
3. Pinia `persist: true` 意味着刷新页面后可以恢复到当前阶段

---

### 5.3 题库分层随机抽样（40/40/20）

> **文件**：`src/data/questions/index.js`

```js
export function selectQuestions({ interviewType, questionCount, difficulty = 'all' }) {
  const typeConfig = interviewTypes[interviewType]
  if (!typeConfig) return []

  const count = questionCount || typeConfig.questionCount

  // ===== 第一步：从指定分类收集题目 =====
  let pool = []
  for (const cat of typeConfig.categories) {
    if (questionsByCategory[cat]) {
      pool = pool.concat(questionsByCategory[cat])
    }
  }

  // ===== 第二步：按难度筛选 =====
  if (difficulty !== 'all') {
    pool = pool.filter((q) => q.difficulty === difficulty)
  }
  if (pool.length === 0) return []

  // ===== 第三步：指定难度 → 直接随机抽取（不分层）=====
  if (difficulty !== 'all') {
    const shuffled = [...pool].sort(() => Math.random() - 0.5)
    return shuffled.slice(0, Math.min(count, pool.length))
  }

  // ===== 第四步：全难度 → 按 40/40/20 分层抽样 =====
  const easy   = pool.filter((q) => q.difficulty === 'easy')
  const medium = pool.filter((q) => q.difficulty === 'medium')
  const hard   = pool.filter((q) => q.difficulty === 'hard')

  // ★ 关键：为什么 hardCount 用减法而不是 Math.round(count * 0.2)？
  //    三个 Math.round 分别取整可能导致总和 ≠ count
  //    例如 count=11: round(11*0.4)=4, round(11*0.4)=4, round(11*0.2)=2
  //    4+4+2=10 ≠ 11！用减法: 11-4-4=3 → 总和精确等于 11
  const easyCount   = Math.round(count * 0.4)        // 40% 简单
  const mediumCount = Math.round(count * 0.4)        // 40% 中等
  const hardCount   = count - easyCount - mediumCount // ~20% 困难

  function pickRandom(arr, n) {
    const shuffled = [...arr].sort(() => Math.random() - 0.5)
    return shuffled.slice(0, Math.min(n, arr.length))
  }

  const selected = [
    ...pickRandom(easy, easyCount),
    ...pickRandom(medium, mediumCount),
    ...pickRandom(hard, hardCount),
  ]

  // ===== 第五步：按难度排序（easy → medium → hard）=====
  const difficultyOrder = { easy: 0, medium: 1, hard: 2 }
  selected.sort(
    (a, b) => difficultyOrder[a.difficulty] - difficultyOrder[b.difficulty]
          || Math.random() - 0.5,        // 同难度随机排列
  )

  // ===== 第六步：某难度池不足时，从剩余题目补足 =====
  if (selected.length < count) {
    const selectedIds = new Set(selected.map((q) => q.id))
    const remaining = pool.filter((q) => !selectedIds.has(q.id))
    const extra = pickRandom(remaining, count - selected.length)
    selected.push(...extra)
  }

  return selected
}
```

**逐段讲解**：

| 步骤 | 解释 |
|------|------|
| 收集 pool | 根据 `interviewType` 的 categories 配置（如 frontend 包含 6 个分类）从各分类聚合题目 |
| 指定难度 | 如果用户选了「困难」就只抽困难的，简单随机不分层 |
| **分层比例** | `4:4:2` 参考真实面试难度分布——基础题为主。用减法算 hardCount 保证总和精确等于 count |
| `pickRandom` | Fisher-Yates 洗牌的简化版。`sort(() => Math.random() - 0.5)` 虽然不是完美均匀分布，但对于题库抽样场景足够了 |
| 排序 | 按 easy → medium → hard 排列，让面试体验从易到难递进 |
| 补足 | 如果 hard 只有 1 题但需要 3 题，用 Set 排除已选中的，从剩余题目随机补足 |

---

### 5.4 AI 多轮追问 + forceComplete 硬约束

> **文件**：`server/routes/interview.js`

```js
// ===== Prompt 模板（关键片段）=====
const EVALUATE_PROMPT = `你是一位资深的前端技术面试官，正在进行深度面试。

## 题目
{question}

## 参考答案要点
{answerPoints}

## 对话历史
{history}

## 评估规则
1. 如果回答深度不够、遗漏关键点、表达模糊，且追问轮次未达到上限，请进行针对性追问。
2. 追问应自然、具体，直击回答中的薄弱环节，不要泛泛而问。
3. 如果回答已经充分覆盖要点，或已达到追问上限（{maxRounds}轮），请给出最终评价。
4. 你当前已追问了 {currentRounds} 轮，上限是 {maxRounds} 轮。{forceComplete}

## 返回格式（仅返回 JSON）
- 追问时：{"action":"follow_up","followUpQuestion":"...","scoreHint":1-10}
- 完成时：{"action":"complete","score":整数,...}`

// ===== 路由处理 =====
router.post("/evaluate", async (req, res) => {
  const { question, answerPoints, conversationHistory = [] } = req.body || {}

  const MAX_ROUNDS = 3
  // ★ 轮次计算：每轮 = 1 条用户回答 + 1 条 AI 追问 = 2 条消息
  const currentRounds = Math.floor(conversationHistory.length / 2)

  // ★★ 关键：动态注入 forceComplete 硬约束
  //    达到上限时，prompt 中插入强指令，否则为空
  const forceComplete = currentRounds >= MAX_ROUNDS
    ? "**已达追问上限，本次**必须**给出最终评价（action=complete），不要继续追问。**"
    : ""   // 未达上限 → 空字符串，AI 自由决策是否追问

  // 组装对话历史文本
  const historyText = conversationHistory
    .map((m) => {
      const label = m.role === "user" ? "考生" : "面试官"
      return `【${label}】${m.content}`
    })
    .join("\n\n")

  // 替换 prompt 模板中的占位符
  const prompt = EVALUATE_PROMPT
    .replace("{question}", safeQuestion)
    .replace("{answerPoints}", pointsText)
    .replace("{history}", historyText)
    .replace(/\{maxRounds\}/g, String(MAX_ROUNDS))       // 正则替换多个占位
    .replace("{currentRounds}", String(currentRounds))
    .replace("{forceComplete}", forceComplete)            // ★ 空或强指令

  // 调用 AI（非流式，温度 0.3 保证输出稳定）
  const result = await callAI({ model, prompt, temperature: 0.3, maxTokens: 600 })

  if (result.action === "follow_up") {
    return res.json({
      action: "follow_up",
      followUpQuestion: result.followUpQuestion,
      scoreHint: result.scoreHint ?? 5,
    })
  }
  // action === "complete"
  res.json({
    action: "complete",
    score: result.score ?? 0,
    correctness: result.correctness ?? 0,
    completeness: result.completeness ?? 0,
    clarity: result.clarity ?? 0,
    feedback: result.feedback || "暂无评价",
    improvedAnswer: result.improvedAnswer || "",
  })
})
```

**讲解要点**：

| 点 | 解释 |
|------|------|
| `Math.floor(conversationHistory.length / 2)` | 每轮产生 2 条消息（用户回答 + AI 追问），除以 2 得到轮次 |
| `forceComplete` 在三元表达式中 | 这是一个**模板注入**模式。`currentRounds >= 3` 时 prompt 末尾多了一段加粗的强制指令；不满足时为空字符串，相当于这个占位符被删除了，AI 自由决策 |
| 为什么放 prompt 里？ | 如果在客户端层面判断"第 3 轮后禁止追问"，AI 可能在第 4 轮继续生成追问指令。把约束直接放 prompt 让**模型自己决策停止**，更可靠 |
| `temperature: 0.3` | 评分场景需要稳定输出，低温度减少随机性。对话场景用默认温度（更高）让回答更自然 |
| Markdown 加粗 `**必须**` | 模型对格式化指令敏感，加粗标记增加注意力权重，提高模型遵循指令的概率 |

---

## 6. 亮点四：知识库系统

### 6.1 一句话概括

> 客户端 `pdfjs-dist` 懒加载 + `mammoth` 解析 PDF/Word，服务端文件系统存储（`index.json` + `meta.json` + `files/*.txt` 三层结构），支持从知识库内容 AI 生成面试题目。

---

### 6.2 客户端：pdfjs-dist 懒加载

> **文件**：`src/utils/docParser.js`

```js
// ★ mammoth 相对较小（~300KB gzipped），直接静态 import
//    几乎所有用户都会上传某种文件，Word 格式也很常见
import mammoth from 'mammoth/mammoth.browser'

// ★ pdfjs-dist 非常大（~2MB 本体 + ~2MB Worker），动态 import
//    只在用户真正上传 PDF 时才加载 —— 很多用户不会上传 PDF
let _pdfJsReady = false
async function ensurePdfJs() {
  if (_pdfJsReady) return                      // ★ 标志位：确保只加载一次
  const [pdfjs, workerSrc] = await Promise.all([
    import('pdfjs-dist'),                      // 动态 import — Vite 会代码分割
    import('pdfjs-dist/build/pdf.worker.min.mjs?url'),
    // ★ ?url 后缀：Vite 把 Worker 文件单独输出为一个独立 URL
    //   不打包进主 bundle，Worker 从独立文件加载
  ])
  pdfjs.GlobalWorkerOptions.workerSrc = workerSrc.default
  _pdfJsReady = true
}

async function parsePdf(file) {
  await ensurePdfJs()                                // ← 首次调用才加载 pdfjs-dist
  const { getDocument } = await import('pdfjs-dist') // 重新拿到模块引用
  const arrayBuffer = await file.arrayBuffer()
  const loadingTask = getDocument({ data: arrayBuffer })
  const pdf = await loadingTask.promise

  const maxPages = 30                                // ★ 限制 30 页，防止超长 PDF
  const total = Math.min(pdf.numPages, maxPages)
  let fullText = ''

  for (let i = 1; i <= total; i++) {
    const page = await pdf.getPage(i)
    const textContent = await page.getTextContent()
    // pdfjs 的文本提取：每页文本是 items 数组，每个 item 有 str 属性
    const pageText = textContent.items.map((item) => item.str || '').join(' ')
    fullText += `\n\n【第 ${i} 页】\n${pageText}`
  }
  return fullText.trim()
}

async function parseDocx(file) {
  const arrayBuffer = await file.arrayBuffer()
  // mammoth.extractRawText — 提取纯文本，丢弃所有格式（加粗、标题等）
  const result = await mammoth.extractRawText({ arrayBuffer })
  return (result?.value || '').trim()
}

export async function parseFile(file) {
  const name = file.name || '未命名文件'
  const ext = getExt(name)             // 取扩展名：.pdf、.docx、.txt ...
  const type = file.type || ''         // MIME type

  try {
    // ★ 根据 MIME type + 扩展名双判断分发到不同解析器
    if (type === 'application/pdf' || ext === '.pdf') {
      const text = await parsePdf(file)
      return { name, text, type: 'pdf' }
    }
    if (type === 'application/vnd.openxmlformats-officedocument.wordprocessingml.document'
        || ext === '.docx') {
      const text = await parseDocx(file)
      return { name, text, type: 'word' }
    }
    // 其它文本类（TXT/MD/JSON/JS/TS...）直接用 File.text()
    const text = await parseTextFile(file)
    return { name, text, type: 'text' }
  } catch (err) {
    throw new Error(`解析文件「${name}」失败：${err?.message || err}`)
  }
}
```

**逐段讲解**：

| 代码 | 解释 |
|------|------|
| `mammoth` 静态 import | mammoth 只有 ~300KB gzipped，Word 是常见上传格式，直接打包进主 bundle 不拖慢首屏 |
| `pdfjs-dist` 动态 import | pdfjs-dist 本体 ~2MB，Worker ~2MB，加起来占首屏 bundle 的很大比例。大多数用户不会上传 PDF，懒加载是合理的 trade-off |
| `_pdfJsReady` 标志位 | 防止重复加载。`ensurePdfJs()` 可能被多次调用（上传多个 PDF），但只需要加载一次 |
| `?url` 后缀 | Vite 特有语法。告诉 Vite 把这个文件单独输出为一个独立 URL（而不是内联为 base64 或打包进 bundle）。Worker 需要在独立线程运行，不能内联 |
| `maxPages = 30` | 防御性限制。有人可能上传一本 500 页的书来生成面试题，30 页足够覆盖核心内容，同时防止 token 溢出和时间过长 |
| `getTextContent()` | pdfjs-dist 的 API。返回值不是纯文本，是 `[{ str: "Hello", ... }, ...]` 数组，需要 `map(item => item.str)` 拼接 |
| MIME + 扩展名双判断 | 有些浏览器/系统不给 MIME type（如 Windows 上 txt 文件可能是空字符串），需要扩展名兜底 |

---

### 6.3 服务端：文件系统存储结构

```
server/data/knowledge/
  ├── index.json                 ← KB 列表 [{id, name, description, fileCount, createdAt}]
  ├── {kb-id-1}/
  │   ├── meta.json              ← {id, name, files: [{id, name, type, charCount, uploadedAt}]}
  │   └── files/
  │       ├── {file-id-a}.txt    ← 纯文本内容
  │       └── {file-id-b}.txt
  └── {kb-id-2}/
      ├── meta.json
      └── files/
          └── {file-id-c}.txt
```

**三层结构说明**：

| 层级 | 文件 | 存储内容 | 为什么这样设计 |
|------|------|---------|---------------|
| KB 列表 | `index.json` | 所有知识库的摘要信息 | 列出所有 KB 时只需读这一个文件，不需要扫描子目录 |
| KB 元数据 | `{kb-id}/meta.json` | 该 KB 的所有文件元信息 | 查文件列表不需要遍历 `files/` 目录 |
| 文件内容 | `{kb-id}/files/{fid}.txt` | 纯文本 | 内容和元数据分离——读元数据时不需要加载文件内容 |

**生成题目的聚合流程**：遍历 `meta.json` 中的 `files` → 逐个读 `files/{fid}.txt` → 拼接 → 截断到 10000 字符（防止超 token 限制）→ 发送给 AI。

**为什么不用数据库？** → 知识库本质是文件的聚合，文件系统天然适合。不需要额外依赖，部署简单。JSON 文件人类可读，方便调试。

---

## 7. 亮点五：工程化细节

### 7.1 Safari ITP 隐私模式 localStorage 兼容

> **文件**：`src/main.js`

```js
// ===== 问题 =====
// Safari 的 Intelligent Tracking Prevention (ITP)
// 在隐私浏览模式下完全阻止 localStorage 读写 → 直接抛 SecurityError
// pinia-plugin-persistedstate 初始化时如果直接调用 localStorage 会崩溃

// ===== 解决方案 =====
const safeLocalStorage = {
  getItem(key) {
    try { return localStorage.getItem(key) } catch (_) { return null }
    // ★ 异常时返回 null — 表现得像 key 不存在，而不是应用崩溃
  },
  setItem(key, value) {
    try { localStorage.setItem(key, value) } catch (_) {}
    // ★ 写入失败静默忽略 — 数据不持久化，但应用继续运行
  },
  removeItem(key) {
    try { localStorage.removeItem(key) } catch (_) {}
  },
}
// 挂到 window 上，供 Pinia persist 配置引用
window.__safeLocalStorage__ = safeLocalStorage
```

**讲解要点**：**"这是优雅降级的设计思想——Safari 隐私模式下存储不可用是环境限制，不是代码 bug。不应该让应用崩溃或弹错误提示打断用户。用 try-catch 包装后，最坏情况是数据不持久化（刷新后丢失），但应用功能完全正常。"**

---

### 7.2 暗色模式防闪烁

> **文件**：`src/main.js`

```js
// ===== 问题 =====
// Vue SPA 的执行流程：HTML 加载 → JS 解析执行 → createApp → mount → 首次渲染
// 如果 mount 之后才从 localStorage 读 isDark 再加 .dark class
// → 浏览器首次 paint 时 class 还没加 → 白屏闪烁

// ===== 解决方案 =====
// ★ 关键：在 createApp() 和 app.mount() 之前同步执行
//    保证浏览器首次 paint 时 html.dark class 已经正确设置

try {
  const raw = localStorage.getItem('app')
  if (raw) {
    const app = JSON.parse(raw)
    if (app.isDark === false) {
      document.documentElement.classList.remove('dark')   // 亮色模式
    } else {
      document.documentElement.classList.add('dark')      // 暗色模式
    }
  } else {
    document.documentElement.classList.add('dark')         // 默认暗色
  }
} catch (_) {
  document.documentElement.classList.add('dark')           // 异常 → 默认暗色
}

// ★ 之后才创建 Vue 应用
const app = createApp(App)
app.use(pinia)
app.use(router)
app.mount('#app')
```

**讲解要点**：**"问题的本质是时序——浏览器的首次渲染发生在 Vue mount 之前。我利用了 JS 是同步阻塞执行的特性：在 mount 之前同步读取 localStorage 并设置 html class。CSS 变量通过 `.dark` 选择器切换值（如 `--bg: #fff` vs `.dark { --bg: #111 }`），首次 paint 时就已经是正确的颜色。这个技巧也被称为 'theme flash prevention' 或 'FOUC prevention'。"**

---

### 7.3 LRU 自动淘汰策略

> **文件**：`src/stores/chat.js`

```js
const MAX_HISTORY = 50             // 最多保留 50 个会话
const MAX_MESSAGES_PER_CHAT = 200  // 每个会话最多 200 条消息

function addToHistory(chat) {
  // ★ 新会话插入头部（unshift）
  history.value.unshift({
    id: chat.id || String(Date.now()),
    title: buildTitleFromContent(chat.title || ''),
    updatedAt: new Date().toISOString(),
  })
  // ★ 尾部溢出则删除 + 同时清理对应 messages，防止内存泄漏
  while (history.value.length > MAX_HISTORY) {
    const removed = history.value.pop()              // pop 尾部
    delete messagesByChatId.value[removed.id]         // ★ 关键：清理消息数据
  }
  return id
}

function addMessage(role, content) {
  // ...
  msgs.push({ role, content })
  while (msgs.length > MAX_MESSAGES_PER_CHAT) msgs.shift()  // shift 头部
  // ...
}
```

**讲解要点**：
- 会话层：`unshift`（头部加新） + `pop`（尾部删旧）= FIFO 队列
- 消息层：`push`（尾部加新） + `shift`（头部删旧）= FIFO 队列
- **关键细节**：淘汰会话时同步 `delete messagesByChatId.value[removed.id]`——否则 messages 会永远留在内存/localStorage 中，造成内存泄漏
- 为什么不用真正的 LRU（按最近访问时间排序）？→ 会话表本身按 `updatedAt` 排序，最旧 ≈ 最少访问，FIFO 效果等价且实现简单

---

### 7.4 5 秒可撤销删除

> **文件**：`src/stores/chat.js`

```js
const undoState = ref(null)  // { chatId, items, insertIndex, timer }

// ===== 删除时保存撤销快照 =====
function _saveUndo(chatId, removedItems, insertIndex) {
  // ★ 新撤销覆盖旧撤销：取消之前的 5 秒定时器
  if (undoState.value?.timer) clearTimeout(undoState.value.timer)

  // ★ 5 秒后自动清空，防止内存泄漏
  const timer = setTimeout(() => {
    undoState.value = null
  }, 5000)

  undoState.value = { chatId, items: removedItems, insertIndex, timer }
}

// ===== 撤销：插回原位 =====
function undoDelete() {
  if (!undoState.value) return
  const { chatId, items, insertIndex } = undoState.value
  const list = messagesByChatId.value[chatId]
  // ★ splice 在原始位置插入 — 消息顺序完全恢复
  if (list) list.splice(insertIndex, 0, ...items)
  undoState.value = null
}
```

> **UI 层**：`src/components/MessageArea.vue`

```html
<Transition name="fade">
  <button
    v-if="chatStore.undoState"
    class="absolute bottom-6 left-1/2 z-10 -translate-x-1/2 ..."
    @click="chatStore.undoDelete()"
  >
    <Icon icon="lucide:undo-2" class="text-primary" />
    <span>已删除，点击撤销</span>
  </button>
</Transition>
```

**讲解要点**：

| 细节 | 解释 |
|------|------|
| 先 clearTimeout 再 setTimeout | 如果短时间内删了两次，第二个删除会取消第一个的定时器——以最新的删除为准，避免旧撤销覆盖新操作 |
| 5 秒自动清空 | 不是永久持有 deleted 数据，5 秒后自动释放。防止 `undoState` 一直持有引用造成内存泄漏 |
| `splice(insertIndex, 0, ...items)` | 在原始位置精确恢复，消息顺序不变。`insertIndex` 是删除时的位置 |
| UI `<Transition name="fade">` | 淡入淡出动画。Toast 出现和消失都有过渡，而不是突然闪现/消失 |
| `v-if` 绑定 `undoState` | undoState 为 null 时按钮完全从 DOM 移除（不是 `display:none`），节省内存 |

---

## 8. 常见追问 & 应答策略

### 虚拟滚动

**Q: 为什么不用 `v-for` + Intersection Observer？**
> 懒加载只让屏幕外的**内容**不加载，DOM 节点还在。几百条消息即使大半 `display:none`，DOM 树仍然巨大，style/layout 计算仍然重。虚拟滚动从根本上**不渲染屏幕外的 DOM 节点**——200 条消息可能只有 10 个节点在 DOM 里。

**Q: 160ms 节流延迟会不会用户感知到？**
> 160ms ≈ 10 帧（60fps），人的视觉感知阈值约 100-200ms。而且 flush 时用的是最新的 `entry.contentRect.height`，不存在"显示过期高度"的问题。用户看到的是平滑的高度增长。

**Q: 如果 `item.size = h` 不触发响应式，为什么界面还会更新？**
> 界面更新分两层：① 文字内容的更新通过 Pinia 响应式驱动（`chatStore.appendToLastMessage`），Vue 会重渲染消息气泡；② 重渲染后 DOM 高度变化，ResizeObserver 检测到 → 更新 sizeMap + item.size → DynamicScroller 读到新的 size 值，调整 item 在虚拟列表中的位置。第②步不需要 Vue 响应式参与——DynamicScroller 自己管理虚拟位置。

**Q: 为什么每个 item 一个 ResizeObserver，而不是一个 Observer observe 所有元素？**
> 单个 item 的挂载/卸载独立管理更清晰——Vue 的 `:ref` 回调自动处理生命周期。如果用一个 Observer，需要自己维护元素列表，每次增删都要手动 observe/unobserve。

### SSE

**Q: 为什么不用 WebSocket？**
> 聊天是单向流（服务端→客户端），SSE 是纯 HTTP，不需要升级握手，天然支持 HTTP/2 多路复用，断线重连浏览器自动处理。WebSocket 适合双向实时通信（如协作编辑）。

**Q: Async Generator 和 Node.js Stream 有什么区别？**
> Stream 是 push 模型——数据推过来，`on('data')` 被动接收。Generator 是 pull 模型——`for await` 主动拉取。Generator 下 try-catch 自然包裹整个过程，错误处理和资源清理更简单。

**Q: 2048B padding 在所有代理下都有效吗？**
> 覆盖绝大多数场景，但极少数 CDN（如 Cloudflare 某些计划）强制缓冲所有 HTTP 响应包括 SSE。这些场景需要升级到 WebSocket 方案。

**Q: _regenerateAbort 为什么不是 ref？**
> 它只有两个操作：set（赋值）和 abort（读值后调用 abort）。不需要响应式追踪，用普通变量零开销。

### 面试引擎

**Q: forceComplete 注入到 prompt 里，AI 真的会听话吗？**
> DeepSeek V4 级别模型对格式化指令很敏感。双重保险：Markdown 加粗 `**必须**` 增加注意力权重 + 给出精确的 JSON action 值 `action=complete`。实际测试中第 3 轮 100% 返回 `complete`。

**Q: 为什么 hardCount 用减法而不是分别 round？**
> 三个 `Math.round` 分别取整可能导致总和 ≠ count（如 11 题：4+4+2=10≠11）。用减法保证精确。

### 工程化

**Q: 50 会话/200 消息够用吗？**
> localStorage 5-10MB 限制。200 条消息约 200-500KB，50 会话约 10-25MB，理论上可能接近上限。但实际用户活跃会话通常就几个，50 是充分冗余。真正的限制是每次启动反序列化整份 JSON 的性能。

**Q: 防闪烁为什么不放在 HTML `<head>` 里？**
> 效果一样。放 `main.js` 顶部是因为 Vite 的 HTML 是模板，维护 JS entry 更方便。本质都是"在首次渲染前同步设置 class"。

---

## 9. 面试模拟：串讲路线图

### 第一步：项目定位（30 秒）
"我做了一个 AI 面试训练平台，基于 Vue 3 + Express + DeepSeek API，支持 SSE 流式对话、AI 多轮追问评分、三种出题模式。在性能和工程化上做了比较深度的优化。"

### 第二步：架构（30 秒）
"前端 Vue 3 + Pinia + Tailwind，后端 Express 薄代理层。AI 调用两条通路——SSE 流式（聊天）和 JSON 非流式（评分）。"

### 第三步：虚拟滚动（2 分钟，核心亮点）
"虚拟滚动是性能优化的重点，我分四层做的：

**第一层数据层**——用 `shallowRef` 而不是 `ref` 持有消息数组。`shallowRef` 只看 `.value` 引用是否变了，不追踪数组内部元素的属性变化。这样流式输出时 content 的修改不会触发数组级响应式。

**第二层测量层**——每个消息 DOM 绑一个独立的 `ResizeObserver`。利用了 Vue 的 `:ref` 回调模式——挂载时创建 Observer，卸载时 `el` 为 null 自动 disconnect。测量时还额外加了 `marginBottom`，因为 DynamicScroller 不自动算 margin。

**第三层节流层**——160ms throttle + RAF。ResizeObserver 在流式输出时非常密集，每个 token 都可能触发。我用 `performance.now()` 计算距上次 flush 的时间差，不足 160ms 就用 setTimeout 等剩余时间再排 RAF。

**第四层更新层是灵魂**——高度存在普通的 `new Map()` 而不是 `reactive Map`，避免 Vue Proxy 开销。更新时 `item.size = h` 直接改数组元素的属性，不替换数组引用。shallowRef 不触发，Vue 不重渲染，但 DynamicScroller 读到了正确的 size 值就能正确布局。

这样整个高度更新流程 Vue 响应式系统完全不参与。"

### 第四步：SSE（1 分钟）
"流式对话服务端用 `async function*` generator 封装 OpenAI SDK，pull 模型有天然的背压。路由里 `for await` 消费 generator，每个 chunk 包成 SSE `data:` 帧。做了一个 2048 字节的防缓冲填充，在 SSE 注释行塞空格强制突破代理缓冲区。

客户端手动消费 `response.body.getReader()`，`TextDecoder({ stream: true })` 防止中文被截断，自己做行缓冲逐行解析。中断恢复用双 AbortController——ChatInput 管发送和继续，MessageArea 管重新生成，通过 `isBusy` computed 互斥。"

### 第五步：其余亮点（1 分钟）
"面试引擎五态状态机 + 40/40/20 分层抽样 + prompt 注入 forceComplete 第 3 轮强制结束。知识库 pdfjs-dist 懒加载，mammoth 解析 Word。工程化上 Safari 隐私模式 localStorage 异常兼容、暗色模式防闪烁、FIFO 淘汰、5 秒可撤销删除。"

### 第六步：收尾（30 秒）
"这个项目帮助我对 Vue 3 响应式系统原理、浏览器渲染机制、SSE 协议、prompt engineering 都有了更深的理解。"

---

## 10. 附录：关键词中英对照表

| 中文 | 英文 | 适用场景 |
|------|------|---------|
| 虚拟滚动 | Virtual Scrolling | 性能优化 |
| 浅层引用 | shallowRef | Vue 3 API |
| 原地变更 | In-place Mutation | 性能模式 |
| 异步生成器 | Async Generator | JS 高级特性 |
| 背压 | Backpressure | 流处理 |
| 中止控制器 | AbortController | 异步控制 |
| 可读流 | ReadableStream | Web API |
| 分层抽样 | Stratified Sampling | 算法 |
| 状态机 | State Machine / FSM | 架构 |
| 硬约束 | Hard Constraint | Prompt Engineering |
| 懒加载 | Lazy Loading / Dynamic Import | 性能 |
| 防闪烁 | Flash Prevention / FOUC | CSS/UX |
| 优雅降级 | Graceful Degradation | 容错 |
| 代理缓冲 | Proxy Buffering | 网络/运维 |
| 布局抖动 | Layout Thrashing | 渲染 |
| 帧对齐 | Frame-aligned (RAF) | 渲染 |
